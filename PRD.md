# 言序（KnowGrove）产品需求文档

> 本文档是 KnowGrove 的产品基线与长期事实来源。后续功能新增、删除、行为调整和发布状态变化，都必须同步修改对应章节，并在文末登记变更记录。

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | KnowGrove |
| 中文名称 | 言序 |
| 产品形态 | Obsidian 桌面端插件 + 浏览器剪藏扩展 |
| 当前产品版本 | 2.8.11（GitHub 正式发布，官方标签预扫描已完成） |
| 本轮目标版本 | 2.8.11 等待 Obsidian 社区目录自动刷新正式版本 |
| PRD 版本 | 1.8.5 |
| 基线日期 | 2026-07-31 |
| 文档状态 | 2.8.11 已正式发布，官方标签预扫描状态 `Completed`，源码 Warning 0、CSS Warning 0、Error 0、Recommendation 0；当前 Obsidian 社区目录展示版本仍为 2.8.9，等待平台自动刷新。必要的文件系统、本地进程、全库枚举和剪贴板能力继续作为准确的桌面能力披露保留，Obsidian 员工人工审核仍待完成 |
| 源代码仓库 | `https://github.com/lufie/KnowGrove`（公开） |

## 2. 产品愿景

KnowGrove 将 Obsidian 从被动保存文件的笔记工具，升级为本地优先的自生长知识工作台：

1. 将网页、视频、语音和手机端轻量笔记稳定收集到 Vault。
2. 自动完成正文提取、转录、总结、属性补齐和阅读状态管理。
3. 用领域、主题、课题、项目、生活空间和 PDSA 循环组织知识。
4. 让用户从一组可信材料中整理证据、生成知识文档和多渠道作品。
5. 保持笔记、引用、评论和生成结果可编辑、可迁移、可追溯。

产品不以维护复杂标签为目标，而是尽可能减少人工配置，让用户主要完成阅读、判断、研究和创作。

## 3. 核心用户与场景

### 3.1 核心用户

- 长期使用 Obsidian 管理资料、研究和项目的知识工作者。
- 需要批量收集网页、视频、录音并继续研究的用户。
- 希望使用本地 CLI 或自有 API 完成 AI 整理，不愿把完整知识库托管到第三方平台的用户。
- 需要从资料库生成公众号、小红书、Medium、X、Reddit 或调研报告的创作者。

### 3.2 核心任务

- 稍后阅读：快速收集，并区分未读和已读。
- 内容入库：从链接或音视频恢复完整、可读、可检索的正文。
- 属性治理：发现缺失或不符合规则的属性，预览后批量修正。
- 知识研究：围绕领域、主题和课题持续聚合相关材料。
- 项目与生活管理：建立项目、子项目、生活目标和例行事项。
- 内容创作：基于选定证据生成可编辑初稿及渠道版本。
- 精读沉淀：评论选中文字，将完整知识块引用到另一篇笔记。

## 4. 产品原则

1. **本地优先**：Vault 是用户内容的最终存储位置。
2. **已有值优先**：自动化默认只补缺失值，不静默覆盖已有属性或正文。
3. **先预览后批量写入**：大范围属性修改必须允许用户确认。
4. **证据可追溯**：研究和创作只能引用真实存在的本地材料路径。
5. **配置最少化**：优先自动检测和推荐默认值，只暴露真正需要用户决策的配置。
6. **Obsidian 原生兼容**：使用 Markdown、YAML、Wikilink、块引用和 Base，不产生 `file:///` 链接。
7. **可恢复**：批量操作失败时尽量回滚，评论和引用数据迁移时不得丢失。
8. **状态如实表达**：已设计、已开发、已部署和已实测必须分开记录。
9. **品牌一致**：面向用户的中文品牌统一为“言序”，英文品牌统一为“KnowGrove”；Obsidian 插件与浏览器剪藏扩展使用言序 App 的暖朱红 `#F24B3F` 作为主交互色，同时继续沿用各宿主的排版、背景和可访问性规则。
10. **消费透明**：云端 AI 必须先估算、后结算、可追溯；失败任务不收费，余额不足不产生负数，付费不能限制用户访问自己的本地笔记。

## 5. 产品信息架构

设置页按以下五个一级模块组织。同层级设置使用 Obsidian 原生设置行，不额外套大卡片边框。

1. 大模型配置
2. Read It Later
3. 属性管理
4. 知识工作台
5. 增强功能

左侧栏提供以下核心入口：

- 阅读列表
- 主题
- 工作台
- 批量存链接
- 录音
- 整理新链接文档

## 6. 功能需求

### 6.1 大模型配置

#### 6.1.1 模型提供方

当前支持以下接入类型：

- Codex CLI
- Claude Code CLI
- Antigravity CLI
- Qoder CLI
- Kimi Code CLI
- MiniMax CLI
- GLM CLI
- CodeBuddy CLI
- Anthropic API
- OpenAI 兼容接口，包括 Ollama、LM Studio 和兼容服务

#### 6.1.2 模型配置体验

- 自动检测本机 CLI 的安装、可执行路径、登录和可调用状态。
- 桌面端自动合并 Obsidian 进程环境、macOS/Linux 登录 Shell `PATH` 和各平台常见安装目录；Claude Code 原生安装路径为 macOS/Linux 的 `~/.local/bin`、Windows 的 `%USERPROFILE%\.local\bin`。
- CLI 检测和模型实际执行必须复用同一个绝对可执行路径，避免终端可用但 Obsidian GUI 内提示命令不存在。
- 区分“可调用”“已安装但不可调用”“待安装或配置”。
- 模型名称优先使用下拉选项。
- 每次检测都从 CLI 自己维护的本机来源动态刷新模型：Codex 读取 CLI 的模型缓存，Qoder 使用 `--list-models`，Kimi 使用结构化 provider 列表，Antigravity 使用模型命令，CodeBuddy、Claude Code、MiniMax 和 GLM 从当前 CLI 帮助或模型参数声明读取。
- CLI 没有提供可脚本读取的完整列表时，合并当前配置模型和安全回退值；不把其他 CLI 的模型名称混入。
- 只有下拉列表不包含目标模型时，用户才填写自定义模型 ID。
- CLI 可执行文件通常留空；只有自动检测失败或安装位置特殊时手动填写。
- 设置页自动展示本机提供方可用状态，并保留“重新检测”入口；不再提供独立的模型测试按钮。
- API Key 使用 Obsidian SecretStorage，不写入 `data.json`。

#### 6.1.3 AI 使用范围

同一模型配置供以下功能复用：

- 新内容属性生成
- 属性工作台 AI 建议
- 网页、视频和语音整理
- 主题与课题分析
- 工作空间资料推荐
- 研究提炼与内容创作

#### 6.1.4 言序云端 AI（规划中）

云端 AI 用于让不了解 CLI、模型 ID 和 API Key 的用户安装后直接使用。当前本地 CLI、自带 API 和本地解析能力继续保留，不能因云端能力上线而被删除或强制迁移。

- 新用户默认看到“言序云端 AI（推荐）”，本地 CLI 和自带 API 收入高级选项。
- 首次使用云端任务时登录言序账号；未登录时可以查看功能与价格，但不能产生付费调用。
- 插件只保存短期访问令牌和刷新令牌，服务商 API Key 只存在于言序服务端。
- 普通用户选择“快速 / 均衡 / 深度”，不直接选择模型 ID。
- 服务端按任务类型、内容长度、历史成功率、价格和可用性进行模型路由，并保留故障切换。
- 属性识别、正文清理、摘要和相关性初筛优先使用 Qwen3.5 Flash。
- 主题分类、资料判断和知识聚类优先使用 Qwen3.5 Plus。
- 跨文档知识合成、知识树和长文创作使用 Qwen3.7 Plus。
- Qwen3.7 Max 不作为默认模型，只能在独立的高成本深度任务中显式启用。
- 非实时属性批处理优先使用 Batch；固定系统提示词优先使用上下文缓存。
- 云端模型路由、单价和降级策略由服务端版本化管理，不把供应商价格硬编码进插件。
- 服务端不能获得任意 Vault 访问权限；插件只发送当前任务明确需要的标题、属性和正文片段。
- 视频和语音默认在本地提取字幕或转录，云端只接收需要整理的文本；原始音视频不默认上传。

##### 首期云服务架构

- 全球 Web、注册页和 API 入口采用 Tencent EdgeOne Makers，使用 Edge Functions 处理鉴权、限流和路由，使用 Cloud Functions 处理账户、订单、模型调用和 Webhook。
- Cloud Functions 分别部署中国大陆和海外区域；首期建议使用广州与新加坡，实际区域以数据库位置、模型服务和真实延迟测试决定。
- 账号、订单、Credit 账本、任务预留和结算使用 Neon PostgreSQL 作为强事务事实源。
- EdgeOne KV 只保存非财务配置、短期缓存和限流状态；由于其跨节点为最终一致性，不能作为余额、账本、支付幂等或订单状态的唯一存储。
- 首期使用 EdgeOne Makers 免费函数额度与 Neon Free 进行技术验证；免费额度、产品政策和可用区可能变化，上线前必须重新核对并配置成本告警。
- 未接入正式模型密钥时，模型网关运行在模拟模式；未接入正式支付商户时，订单运行在沙盒或模拟支付模式，不能向真实用户收款。

##### 域名与中国大陆访问

- 开发环境先使用平台分配地址，不购买域名。
- 正式域名候选为 `knowgrove.app`；购买前再次检查可用性、续费价格和品牌风险。
- EdgeOne 包含中国大陆节点的正式自定义域名需要按平台和监管要求完成 ICP 备案。
- 未完成自有域名和备案前，只能描述为全球测试环境，不能承诺中国大陆正式稳定访问。
- 域名、API 地址和登录回调地址必须由服务端配置下发，不能硬编码在插件业务逻辑中。

#### 6.1.5 Credit 定义与计费（规划中）

##### Credit 基础定义

- 1 Credit 对应最高 `¥0.001` 的模型调用成本，是言序内部成本计量单位，不等同于现金、Token 或可提现资产。
- 供应商实际账单中的输入 Token、输出 Token、思考 Token、缓存命中和 Batch 折扣统一换算为 Credit。
- 首期成本基线采用模型官方原价，不使用限时优惠测算长期毛利。
- 以当前模型原价估算，每 1,000 Token 的 Credit 基准为：

| 模型 | 输入 | 输出（含思考 Token） |
| --- | ---: | ---: |
| Qwen3.5 Flash | 0.2 Credit | 2 Credits |
| Qwen3.5 Plus | 0.8 Credit | 4.8 Credits |
| Qwen3.7 Plus | 2 Credits | 8 Credits |

供应商调价后，由服务端发布新的版本化费率表；已完成任务不能使用新费率追溯重算。

##### 预估、预扣与结算

1. 插件提交任务类型、输入规模和唯一幂等键。
2. 服务端根据最大输入、最大输出、目标模型和安全余量返回预计 Credit 区间。
3. 余额充足时把预计上限转入“预留余额”，但尚不计为已消费。
4. 模型完成后按供应商返回的真实 usage 结算，未使用的预留 Credit 立即释放。
5. 请求重试、模型降级和故障切换必须属于同一个任务，不得重复扣费。
6. 服务端未返回可用结果时全额退回；用户接受部分结果时只按实际 usage 结算。
7. 余额不足时不启动付费调用，不产生负数余额，也不默认自动充值。

##### Credit 账本

- 服务端余额以不可变、只追加的账本为唯一事实来源，不能只保存一个可直接修改的余额数字。
- 账本事件至少包含：`trial_grant`、`subscription_grant`、`purchase_grant`、`reserve`、`consume`、`release`、`refund`、`expire`、`chargeback` 和 `manual_adjustment`。
- 每条事件记录用户、订单或任务、Credit 数量、费率版本、时间和幂等键。
- 可用余额 = 已生效赠送与购买 - 已消费 - 已预留 - 已过期 - 已退款或拒付撤销。
- 消费优先级：最早到期的体验或月度 Credit → 后续到期 Credit → 用户购买的长期 Credit。
- 用户可以在用量明细中查看每项任务的任务名称、预估、实际消费、退款和余额变化；不要求理解 Token。

#### 6.1.6 套餐与毛利约束（规划中）

首发只突出低门槛套餐，重度用户通过加量包或后续专业套餐承接。

| 套餐 | 价格 | Credit | 有效期 | 目标用户 |
| --- | ---: | ---: | --- | --- |
| 首次体验 | 免费 | 100 | 领取后 30 天 | 验证首次云端任务 |
| 基础版 | `¥9.9/月` | 1,300/月 | 当期有效，不结转 | 日常剪藏、属性和轻量整理 |
| 标准版 | `¥19.9/月` | 2,800/月 | 当期有效，不结转 | 高频整理、主题分析和少量长文 |
| 加量包 | `¥9.9` | 1,000 | 购买后长期有效，按支付与监管要求展示有效规则 | 超出月度额度的临时需求 |

- 基础版在用户完全消耗额度时，模型成本上限约 `¥1.30`；标准版约 `¥2.80`。
- 以支付手续费 3%、基础云服务分摊约 `¥0.30 / ¥0.50` 估算，基础版单用户月毛利润约 `¥8.00`、毛利率约 80.8%；标准版月毛利润约 `¥16.00`、毛利率约 80.4%。
- 上述为毛利润，不包含研发工资、客服、税费和市场推广；上线前必须使用真实账单复核。
- 商业目标是整体付费用户组合毛利率不低于 80%，不能依靠隐藏扣费、无法取消或故意让用户遗忘余额实现。
- 当供应商价格、支付费率或模型路由变化导致预测毛利低于 75% 时触发经营告警，不静默降低模型质量或缩减用户已购权益。

#### 6.1.7 用户购买与付款链路（规划中）

##### 账号与发起购买

1. 用户在“大模型配置”看到云端 AI 状态、当前 Credit 和“充值 / 管理套餐”入口。
2. 未登录用户点击后，通过系统浏览器打开言序 HTTPS 登录页，使用一次性设备码完成配对。
3. 登录成功后，访问令牌进入 Obsidian SecretStorage，不写入 `data.json`，不进入 Vault 同步。
4. 用户选择基础版、标准版或加量包；插件向服务端创建订单，服务端返回订单号、金额、Credit、权益期限和支付页地址。
5. 插件只打开支付平台托管页，不收集银行卡号、支付密码或完整支付凭证。

##### 仅邮箱登录

- 言序云端账号只支持邮箱，不提供手机号、微信、Google、GitHub 或其他社交登录。
- 注册流程为：填写邮箱、密码和称呼 → 接收 6 位验证码 → 验证邮箱 → 创建网页会话；验证码未通过前账号不可用于付费调用。
- 登录流程为邮箱与密码；错误信息不能暴露邮箱是否已经注册，防止账号枚举。
- 忘记密码流程为：提交邮箱 → 接收一次性重置链接或验证码 → 设置新密码 → 撤销该账号已有刷新令牌。
- 邮箱验证码和密码重置凭证必须单次使用、短期有效、只保存哈希，并限制同邮箱、IP 和设备的发送与验证频率。
- 密码哈希、用户、会话、邮箱验证状态和重置记录由 Neon Auth 管理，业务表不保存明文密码或重复密码字段。
- 开发环境可以使用 Neon Auth 共享邮件服务；正式上线前必须切换为言序自己的发信域名与邮件服务，并完成 SPF、DKIM 和 DMARC。
- 用户可以查看最近登录设备、撤销单个会话或退出所有设备；密码重置后默认退出其他设备。
- Obsidian 插件不显示密码输入框，只生成设备码并打开网页；用户在已登录网页确认后，插件换取短期访问令牌和可轮换刷新令牌。
- 设备码短期有效、只能确认一次；插件轮询必须限速，拒绝的配对不能再次兑换令牌。

##### 支付、到账与恢复

1. 支付平台处理付款并向言序服务端发送签名 Webhook。
2. 服务端先验签，再按支付平台交易号和言序订单号做幂等处理。
3. 已支付订单写入订单状态，并向 Credit 账本追加 `subscription_grant` 或 `purchase_grant`。
4. 插件轮询订单状态或在重新获得焦点后刷新；到账后显示新增 Credit、当前余额和交易时间。
5. 用户关闭插件或浏览器不会丢失订单；重新打开后可通过订单号恢复状态。
6. 重复 Webhook、网络重试和多设备同时登录不得重复发放 Credit。

订单状态固定为：

`created → pending_payment → paid → credited`

异常终态为：

`payment_failed`、`closed`、`refund_pending`、`refunded`、`chargeback`

##### 退款、拒付与订阅

- 退款申请由言序服务端查询订单和对应账本，不在插件本地直接修改余额。
- 未消费的购买 Credit 按适用支付规则撤销后退款；已消费部分不能再次作为可用余额。
- 拒付成立后追加 `chargeback` 事件；余额不足以撤销时冻结新的云端付费任务，但不能删除或锁定用户本地笔记。
- 月度套餐取消后当期权益持续到周期结束，不再续费；用户仍可使用本地 CLI、自带 API 和本地解析能力。
- 自动续费、取消入口、下次扣款时间和价格必须在购买前与设置页中持续可见。

#### 6.1.8 余额、预算与消费体验（规划中）

- 设置页显示当前可用、已预留、当月已消费和下次发放 Credit。
- 自动整理新内容可以使用云端 AI，但必须遵守用户设置的月度自动消费上限。
- 默认关闭自动充值；后续如提供，必须由用户单独开启并设置单次与每月上限。
- 余额达到 80% 用量时提醒一次；余额不足时暂停云端 AI，不影响本地阅读、编辑、解析和 CLI。
- 小额自动任务在预算内直接执行；跨文档创作、批量分析和预计高于 100 Credits 的任务在执行前展示预计范围。
- 每个结果页面都能查看本次实际 Credit，并能跳转到完整用量明细。
- 云端任务失败时必须展示“未扣费 / 已退回 / 已按实际用量结算”中的一个明确状态，不能只显示通用错误。

#### 6.1.9 云端商业化验收标准（规划中）

- 支付成功到 Credit 可用的 P95 时间不超过 10 秒。
- 同一订单重复回调 100 次仍只发放一次 Credit。
- 同一 AI 任务网络重试不重复消费，账本与供应商 usage 可对账。
- 无可用模型结果的失败任务 100% 释放预留 Credit。
- 插件卸载、重装和多设备登录后，服务端余额与订单不丢失。
- 插件包、`data.json`、日志和崩溃报告中不存在服务商密钥、支付凭证或完整正文。
- 云端 AI 上线前使用至少 200 个真实脱敏任务比较 Flash、3.5 Plus 和 3.7 Plus 的属性准确率、JSON 成功率、摘要保真度、主题一致性、长文可用率和重试率。
- 只有评测达标的任务才下放到低成本模型；低置信度和失败任务自动升级，但必须受预估上限约束。
- 使用首个完整结算周期的真实模型账单、支付费率和云资源账单复核套餐，整体付费用户毛利率目标不低于 80%。

### 6.2 Read It Later

#### 6.2.1 收集箱

- 用户只需确认一个 Vault 相对路径。
- 阅读列表、浏览器剪藏、手机端剪藏、轻量链接解析和新资料属性初始化共用该路径。
- 当前用户配置为 `Home/📬输入`。

#### 6.2.2 阅读状态

- 只保留未读和已读两个主要分类。
- 未读、已读和刷新操作在窄侧栏中保持同一行且互不覆盖；空间不足时，数量自动移到分类名称下方。
- 新进入收集箱的 Markdown 默认标记为未读。
- 用户可手动切换阅读状态。
- 可在阅读到文末并停留指定时间后自动标记已读。
- 编辑正文时暂停自动完成，避免误判。
- 阅读列表高亮当前正在阅读的文档。

#### 6.2.3 最近文件

- 不创建新的侧栏标签，也不在 Vault 中创建真实“最近”文件夹。
- 隐藏左侧栏原生的“新标签页”新增按钮，主编辑区的新标签页按钮保持可用。
- 在 Obsidian 原生“文件列表”顶部挂载一个可折叠的虚拟“最近”分类，默认展开。
- 与 `_KnowGrove`、`Home` 等根目录保持同级缩进和原生透明背景，不形成独立卡片或副层级。
- 标题、箭头和展开状态复用原生文件夹结构；箭头方向与旋转动效和 `Home` 等根目录保持一致，折叠时不重建列表。
- 用户可在“增强功能”设置依据为“最近操作”“最近编辑”或“最近新建”；“最近操作”按 Obsidian 最近打开记录排序。
- 用户可在“增强功能”设置显示数量，范围为 3–20 篇，默认 8 篇。
- 只展示 Markdown、Base 或 Canvas，过滤音频、图片等附件。
- 点击条目沿用 Obsidian 打开文件行为；当前文件沿用原生选中样式。
- Obsidian 不提供公开的文件树虚拟目录接口，插件使用范围受限的界面挂载，并在文件树重绘后自动恢复；若内部最近历史接口不可用，则回退为最近修改的文档。

#### 6.2.4 Mac 链接存储与录音

- 桌面端 Ribbon 分别提供“批量存链接”和“录音”两个独立按钮，不要求用户先创建空白笔记，也不再经过统一收集页或页内标签切换。
- “批量存链接”继续使用轻量弹窗，不在左侧标签索引中新增条目；“录音”打开工作台下方的临时左侧标签，用户可像普通侧栏页一样关闭，不加入永久核心入口。
- 两个入口直接进入各自的操作界面，不重复展示“收集”标题、副标题或功能名称；仅保留完成任务所需的简短提示。
- 录音工作区在空闲状态增加“导入音视频”入口，用户可以拖拽或点击选择一个或多个本地文件；不新增 Ribbon 或永久侧栏条目。
- 导入入口明确展示支持格式：音频 `mp3`、`m4a`、`wav`、`aac`、`flac`、`ogg`、`opus`、`webm`；视频 `mp4`、`mov`、`mkv`、`m4v`。不支持的文件不进入队列。
- 外部大文件使用本地文件系统复制到 Vault 的音视频附件目录，不把整段视频一次性读入渲染进程内存；已位于当前 Vault 的媒体直接复用，不重复复制。文件重名时生成安全的新名称，绝不覆盖已有文件。
- 每个导入文件生成一篇独立轻量 Markdown，使用 Obsidian Wikilink 引用媒体并立即进入现有 Whisper 与 AI 整理队列；结果列表展示复制中、后台解析、完成或失败状态，并可随时打开对应笔记。失败时保留已导入的媒体和笔记，允许从笔记菜单重新解析。
- 批量链接允许单篇、多篇或链接与说明文字混合粘贴；系统提取所有唯一的 HTTP/HTTPS 链接，每个链接创建一篇独立 Markdown，并直接进入现有网页、视频或语音解析队列。
- 链接保存后逐篇展示对应笔记入口；即使解析仍在后台进行，用户也能立即打开已安全写入 Vault 的笔记，文件后续重命名时入口继续跟随同一 TFile。
- 批量链接默认存入收集箱，也可单独配置 Vault 相对目录。
- 录音可填写标题，也可使用本地时间生成默认标题；录音文件、Markdown 笔记和安全恢复片段默认保存到“收集箱路径/录音”，也可单独配置。
- 录音页不显示手动“收起为悬浮框”按钮；录制中切换到其他侧栏标签或关闭录音页时，自动显示小型悬浮框，返回录音页时自动隐藏。
- 悬浮框持续展示录制时长、当前状态、“返回录音”和停止入口；可拖动到窗口左右任意位置并记住最近位置，恢复按钮使用文字与原生侧栏图标表达目标。
- MediaRecorder 每 2 秒产生数据块并追加到本地安全片段，每次写入执行文件同步；每 10 分钟轮换安全片段，并使用原子 `manifest.json` 记录会话、片段和中断状态。
- 用户主动停止后，单个安全片段直接保存原始编码，不再等待 AAC 转码；多个同格式片段优先通过 FFmpeg 流复制快速合并，仅在格式不兼容或流复制失败时回退为 AAC 转码。音频封口、基础属性和 Markdown 笔记写入完成后立即显示“录音已安全保存”并恢复交互；AI 语义属性、转录和内容整理转入后台，不能阻塞停止保存。单片段只显示“正在保存录音”，仅多片段才显示“正在合并并保存录音”。正文使用 Obsidian Wikilink 嵌入，不产生 `file:///` 链接。
- Obsidian 意外退出时保留未完成会话；再次打开只提示恢复，不自动启用麦克风。用户明确点击继续或保存后才访问麦克风或完成合并。
- macOS 电话接力或其他应用占用麦克风时，以媒体轨道 `mute` / `ended` 和音频设备变化为准：先保存当前片段，快速重试后转为低频等待；系统归还麦克风后继续同一个录音会话。若系统未占用当前输入设备，录音保持连续。
- 插件不能控制 iPhone 电话状态，也不宣称绕过 macOS 的麦克风独占和隐私权限；首次录音由系统弹出麦克风授权。

#### 6.2.5 浏览器剪藏

- Chrome 或 Safari 扩展提交当前页面。
- 插件内本机服务只监听 `127.0.0.1:47831`。
- 首次连接必须由用户在 Obsidian 中确认配对。
- 扩展只负责页面识别、提交和进度展示。
- 下载、解析、AI 调用和 Vault 写入全部由 KnowGrove 完成。
- 用户可以撤销当前电脑上的浏览器授权。

#### 6.2.6 手机端剪藏

- 手机或其他采集工具只需在收集箱中创建一篇轻量 Markdown。
- 当内容只有一个链接、标题和少量说明时，插件识别为待解析笔记。
- 已有完整正文、多链接或已处理笔记不会被自动改写。

#### 6.2.7 自动整理新内容

该开关是新内容自动化的统一触发器：

- 给新资料补齐基础属性和阅读状态。
- 对轻量链接或语音笔记执行解析、转录和 AI 整理。
- 语音笔记只有一个本地音频 Wikilink 和少量模板文字时，也视为待整理内容；不要求出现 HTTP 链接。
- 录音应用可以先创建笔记、数小时后再补写音频引用；插件监听后续修改并重新判断，不把识别窗口限制在文件刚创建后的数秒内。
- 开启 AI 自动属性后，继续生成类型、状态、领域和主题等语义属性。
- Obsidian 启动时补查最近遗漏的新文档。
- 用户也可点击左侧“整理”按钮或命令面板手动触发。

#### 6.2.8 网页文章解析

- 链接先跟随 HTTP 重定向，短链使用最终地址、响应类型和页面元信息重新识别内容类型。
- 不依赖站点白名单：无法识别为音视频时，按网页内容尝试本地正文提取。
- 浏览器剪藏优先读取当前标签页已渲染的正文、选中文本或 AI 对话区域；适用于豆包等动态分享页。
- 手机端只有链接时，除正文提取外还会尝试读取页面内嵌的结构化 JSON；仍无公开正文时保留原链接和可重试状态。
- 使用内置解析能力和 Defuddle 提取正文。
- 删除作者栏、编辑提示、关注引导、二维码、推荐阅读、转载声明和平台装饰符号。
- 不把头图当作正文图片保存。
- 正文图片下载到 Vault，并使用 Obsidian 内部链接嵌入。
- 保留论证、数据限定、原意和图片顺序。
- 文件名属性、Markdown 文件名和正文标题保持一致。
- 可选择生成 `YYYY-MM-DD-文章名` 格式，便于按文件名排序。

#### 6.2.9 视频解析

- 除已知平台外，响应类型、最终地址、Open Graph / Twitter Player 元信息或页面视频元素均可触发视频链路。
- 优先读取视频字幕，字幕语言不写死：根据平台实际返回的语言列表动态选择简体中文、AI 中文、中文、繁体中文、英文或其他可用字幕，并排除弹幕。
- 从浏览器扩展剪藏 Bilibili 时，扩展在当前登录页面内调用 Bilibili 官方播放器字幕接口；浏览器 Cookie 不会被读取、保存或发送给 KnowGrove。
- 手机端或轻量链接自动整理无法获得登录字幕时，先尝试公开视频字幕，再进入音频下载与本地转录。
- 没有字幕时，使用 `yt-dlp` 下载公开音频。
- Bilibili 请求使用公开页面的浏览器请求头和有限重试，不读取或上传浏览器 Cookie。
- `yt-dlp` 的过期警告不会覆盖真正错误；HTTP 412、登录限制和公开访问失败显示可执行的中文提示。
- 点击“自动配置”时，如果签名清单中的 Runtime 比当前托管版本新，必须下载并原子升级，不能因为旧组件仍可执行而直接跳过。
- 多个签名清单源同时可用时，插件选择 Runtime 版本最高的清单，避免较旧主镜像阻挡备用源的新组件。
- 使用 FFmpeg 和 Whisper 生成逐字稿。
- 本地视频引用支持 `mp4`、`mov`、`mkv` 和 `m4v`，与本地语音共用音轨提取、Whisper 自动语种识别和 AI 整理链路。
- 字幕或 Whisper 逐字稿始终保留实际说话语言，不因系统语言不同而翻译、改写或音译；系统语言只决定插件生成的摘要、要点、整理正文和分析章节标题。
- 字幕和 Whisper 的时间切片先合并为可读段落；AI 再按语义生成摘要、要点、主题小标题和自然段，禁止一条字幕占一行。
- 不绕过登录墙、付费限制或 DRM。

#### 6.2.10 语音解析

- 除已知平台外，响应类型、最终地址、页面元信息或音频元素均可触发语音链路。
- 收集箱中的本地音频引用支持 `mp3`、`m4a`、`wav`、`aac`、`flac`、`ogg`、`opus` 和桌面录音产生的 `webm`；通过 Obsidian Wikilink 解析附件位置，不生成 `file:///` 绝对链接，也不重复复制已有音频；桌面本地文件系统优先把现有文件路径直接交给 FFmpeg/Whisper，避免将长录音完整读入渲染进程内存；整理后的“原始音频”章节继续保留可播放的 Wikilink 嵌入。
- 优先读取平台提供的字幕或转录文本；没有可用文本时再下载公开音频。
- 下载转录时原始语音文件保存到 Vault。
- 使用 Whisper 生成逐字稿。
- Whisper 自动识别实际说话语言，完整逐字稿按原语言保存；例如中文系统中的英文对话仍保留英文原文。
- AI 生成摘要、要点、对话整理和行动信息，所有分析内容及插件生成的章节标题必须使用本地系统语言；例如中文系统中的英文录音使用英文逐字稿和中文分析。
- 语言规则不改写治理属性键、用户已有 Frontmatter 或原始音视频 Wikilink。
- 说话人不确定时使用中性编号，不猜测姓名。
- 用户需要删除原始语音时，直接在 Vault 中手动删除。

#### 6.2.11 运行组件检测

设置页展示：

- 网页文章解析可用或不可用
- 视频解析可用或不可用
- 语音转录可用或不可用

提供“检测”和“自动配置”两个操作。

### 6.3 属性管理

#### 6.3.1 属性分层

KnowGrove 使用四层属性方法：

1. 领域树：稳定、长期、最多两级。
2. 区块维度：类型、内容类型、所属项目、发布渠道等正交属性。
3. 概念索引：主题、来源笔记和关联笔记。
4. 生命周期：通过 PDSA 推动计划、实践、研究和应用。

#### 6.3.2 基础属性规则

外部输入资料至少维护：

- 文件名
- 类型
- 状态
- 创建时间
- 领域
- 主题
- 阅读状态

规则字段由插件确定性维护；语义字段可由 AI 建议。未知属性完整保留。

#### 6.3.3 AI 自动属性

- AI 只生成启用的语义字段。
- 默认处理类型、状态、领域和主题。
- 已有值默认保留。
- 开关关闭后，工作台仍能执行确定性规则扫描，但不能生成 AI 建议。

#### 6.3.4 忽略文件夹

- 属性扫描默认覆盖整个 Vault。
- 自动跳过插件数据、系统文件和代码依赖。
- 用户可按行添加额外忽略目录。
- 忽略目录之外的 Markdown 才进入属性工作台扫描。

#### 6.3.5 属性工作台

- 显示符合规范和待规范数量。
- “重新检查”只扫描，不修改笔记。
- “处理”打开属性修正表格。
- 表格每行对应一篇文章，每列对应一个需要检查的属性。
- 颜色区分无需修改和需要修正。
- AI 可生成建议值，用户可逐格编辑。
- 用户统一确认后才写入。
- 文件名属性自动与 Markdown 文件名同步，不占用修正表格。
- 批量处理显示总数、完成数和失败数；完成后可关闭进度提示。

#### 6.3.6 分类树建议

- AI 扫描已有标题和分类摘要，生成一套领域树建议。
- 用户可直接采用、暂不使用或微调。
- 领域最多两级。
- 采用方案只更新规则，不立即批量重写所有笔记。

### 6.4 知识工作台

#### 6.4.1 研究知识树

- 领域下展示主题。
- 主题下展示课题和相关材料。
- 主题、课题支持展开与收起。
- 创建重复主题或课题时提示已存在。
- 修改主题或归属时，同步修改对应笔记属性。

#### 6.4.2 全局主题索引

- 在左侧栏提供独立“主题”入口，与阅读列表、工作台并列；不复刻 Obsidian 原生标签页。
- “增强功能”提供“主题列表”总开关，默认开启；关闭时隐藏侧栏和 Ribbon 入口并关闭已有主题视图，但不修改或删除任何笔记属性、主题工作区或关联数据。
- 索引直接读取所有治理范围内笔记的 `主题` 属性，不使用工作台的聚类门槛；即使一个主题只出现在一篇笔记中，也必须展示。
- 默认平铺全部主题，不按领域、文件夹、名称前缀或其他推断结果做二次聚合。
- 只有主题工作区明确设置 `上级主题` 时才按父子层级缩进展示；清空 `上级主题` 后立即恢复为顶层主题。无效、缺失或循环的父级关系不得隐藏任何主题。
- 主题设置支持从已有主题中选择可选的上级主题，并提供“无（顶层主题）”；保存前必须阻止主题指向自身或形成循环。父主题重命名时同步更新子主题关系，父主题删除时子主题自动回到顶层。
- 支持按主题名称、领域、关联文档标题和路径搜索。
- 点击主题后展示全部真实属性关联文档，并高亮当前打开文档；点击文档直接打开。
- 正式知识树主题标记为“已进入知识树”；原始笔记主题标记为“尚未进入知识树”，并提供加入知识树入口。
- 主题与文档关系建立为一次索引并缓存复用，禁止在每次点击或输入时重新执行主题数乘文档数的全量匹配。
- 主题列表一次最多渲染 80 个主题，主题详情一次最多渲染 40 篇关联文档，用户可继续加载后续内容；搜索命中子主题时同时保留其显式父级上下文。
- 主题列表和文档详情保留明确的垂直间距；文档按“标题 + 路径”两行展示，不把两者挤在同一行。
- 主题详情支持批量重命名：确认新名称后，同步更新所有关联笔记的 `主题` 属性，并保留同一笔记里的其他主题。
- 主题详情支持删除：确认后从全部关联笔记移除该主题；来源文档不删除，正式主题的工作区和课题目录移入 Obsidian 回收站。
- 浏览和搜索不自动修改笔记；主题改名、删除和属性写回只由主题详情或工作台中的明确确认操作触发。

#### 6.4.3 资料关联

- 从整个非排除 Vault 中寻找与主题或课题相关的候选材料。
- 候选资料不写入笔记 frontmatter。
- 候选状态存储在插件管理的 sidecar 数据中。
- 用户可标记相关、不相关或暂待判断。
- 被拒绝的资料后续不再展示。
- 点击候选资料可在分栏中阅读，并保持目标主题文档可见。

#### 6.4.4 研究课题

- 课题是主题下面可持续研究的具体问题。
- 每个课题生成可编辑 Markdown 工作区和对应 Base。
- 支持固定资料、候选资料、研究问题、证据整理和主题综合。
- 资料路径必须来自真实 Vault 文件，AI 不得编造来源。

#### 6.4.5 PDSA 知识循环

- P：明确研究计划和问题。
- D：收集资料、实践和记录。
- S：研究、综合、建立概念和证据结构。
- A：应用、输出、复盘并进入下一轮。

PDSA 是围绕主题推动知识生长的生命周期，不等同于阅读状态。

#### 6.4.6 项目空间

- 支持项目和子项目。
- 项目包含目标、资料、行动、交付和复盘。
- AI 可推荐与项目目标直接相关的本地材料。
- 项目分类与研究主题可以关联，但不强制互相替代。

#### 6.4.7 生活空间

- 支持生活目标和例行事项。
- 可组织目标、执行记录、资料和复盘。
- 生活空间使用相同的领域、主题和资料关联机制。

### 6.5 研究与内容创作

#### 6.5.1 材料选择

- 用户可从主题、课题、工作空间或手动选择一组本地资料。
- 长材料按块提炼，不静默忽略超过批次上限的资料。
- 提炼结果保留精确来源路径和证据边界。

#### 6.5.2 生成流程

1. 选择材料。
2. 提炼证据。
3. 生成可编辑大纲。
4. 按章节生成初稿。
5. 用户在 Obsidian 中修改。
6. 生成其他渠道版本或重新生成局部章节。

#### 6.5.3 输出模板

当前支持：

- 微信公众号
- 小红书
- Medium
- X / Twitter
- Reddit
- 调研报告
- 内容大纲

#### 6.5.4 作品管理

- 作品保存到用户指定的 Vault 文件夹。
- 用户可直接输入路径或从现有文件夹中选择。
- 首稿、渠道稿、版本记录和来源关系保存在 Vault 与 sidecar 中。
- 不覆盖来源资料。

#### 6.5.5 配图

- 可关闭真实图片生成，只保留配图方案。
- 开启后通过配置的图片模型生成图片，并保存为 Vault 附件。
- 图片方案必须与文章章节和证据一致。

### 6.6 评论与块引用

#### 6.6.1 评论

- 用户选中文字后，只在选区右侧显示评论图标。
- 点击后在右侧评论栏编辑。
- 多条评论可定位回对应原文。
- 预览模式下，被评论文字显示下划线。
- 删除评论后，下划线同步消失。
- 添加到目标文档时，内容写入目标文档末尾的“评论”章节。

#### 6.6.2 整块拖动引用

- 选中文字后可拖到另一篇 Markdown。
- 插件自动扩展到选区所在的完整 Markdown 块。
- 目标文档使用 Obsidian 原生块嵌入展示。
- 源内容修改后，目标笔记同步展示最新内容。
- 不生成绝对路径或 `file:///` 链接。

#### 6.6.3 数据持久化

- 评论和引用记录保存在插件 `data.json` 的 `references` 中。
- 源块使用稳定 Block ID。
- 目标文档保存可迁移的 Wikilink 和受管理标记。
- 插件重装或电脑同步时，只要同步 Vault 插件数据即可恢复。

### 6.7 增强功能

- 主题列表：默认开启；关闭只隐藏全局主题入口，不修改主题数据。
- 删除多余空行：段落间保留一个空行，默认关闭。
- 选中文字支持整块拖动：默认开启。
- 启用评论：默认开启。
- 类 Word 实时编辑：默认开启，只作用于 Obsidian 实时预览，源码模式保持原样。
- 附件冗余检测：默认开启，只检测并提醒，任何附件删除都需要用户明确确认。

#### 6.7.1 类 Word 实时编辑规范

**目标**

- 用户编辑排版后的文字时，继续看到标题、粗体、斜体、高亮、删除线、行内代码和双链的实际样式，不因光标进入而切回 Markdown 标记。
- 内容仍以标准 Markdown 保存；插件只改变编辑呈现和键盘语义，不把 Vault 转换为私有富文本格式。
- 普通输入、撤销、重做、复制、粘贴、选区修改和 Obsidian 原生命令继续即时生效。

**格式呈现**

- 在实时预览中持续隐藏标题 `#`、粗体与斜体标记、高亮、删除线、行内代码反引号、转义符和双链两侧标记。
- 隐藏标记被视为不可落入光标的边界；点击标记附近时，光标落到可编辑正文一侧。
- YAML、代码围栏、数学块、HTML 和未完整闭合的 Markdown 保持原始编辑形态，避免隐藏语法后无法修复。
- 源码模式不启用隐藏或键盘接管，用户始终可以切回源码完成精确修改。

**块级键盘行为**

- 标题：在标题正文起点按退格先移除标题格式并保留文字；空标题按回车恢复普通段落；非空标题末尾按回车创建普通正文段落。
- 无序与有序列表：列表标记和其后的空格是不可拆分的编辑边界，点击标记区域时光标落到正文起点，不允许光标停在符号与空格之间。
- 列表回车：非空项中或末尾按回车，在相同层级创建同类型列表项；有序列表自动续号并重排同级后续项。空白根级列表按回车退出列表；空白嵌套列表按回车只降低一级，不一次跳回普通正文。
- 列表退格：光标位于正文起点时，每按一次退格只降低一个层级，并带动该项已有子树保持相对层级；到达根级后再按一次才移除列表格式，正文不得丢失。
- 列表层级：根级之外，Tab 只增加一级，Shift+Tab 只减少一级；缩进宽度从当前连续列表块中识别，无法识别时跟随编辑器 Tab 宽度，空格列表与 Tab 列表分别保持原有缩进风格。
- 列表删除：同层级相邻项在前一项末尾按 Delete 时合并为一项；有序列表合并、插入或升降级后必须自动关闭编号缺口和重复编号。任务列表持续显示可交互复选框，即使整行或跨行选中也不展开 `- [ ]` 源码；勾选仍写回标准 Markdown 任务状态。
- 引用：在引用正文起点按退格先移除引用格式；空引用按回车恢复普通段落。
- 图片与媒体：单独占一块的 `![[...]]` 或 `![](...)` 在实时预览中点击后按整块选中；在媒体块本身按退格或删除，或在其相邻空段落朝媒体方向按键，删除完整媒体引用，不留下半截语法。
- 围栏代码块：光标进入代码块内部时，输入、回车、退格和删除全部保持 Obsidian 原生源码编辑；光标位于代码块前后的空段落时，朝代码块方向按 Delete 或 Backspace，一次删除从开启围栏到匹配关闭围栏的完整代码块。只处理完整闭合且围栏字符、长度匹配的反引号或波浪线代码块，未闭合代码不得自动删除。
- 任务列表：选择、回车、退格、Delete、Tab 与 Shift+Tab 都把列表标记和复选框当成一个不可拆分前缀；非空项回车创建同层级未完成任务，空根级任务退格或回车退出列表，空嵌套任务逐级退出，任务合并和层级移动不得留下 `- [`、`- [ ]` 等残缺标记。普通段落、表格和数学块若没有上述明确规则，则交给 Obsidian 原生编辑器处理。

**兼容与安全边界**

- 功能提供一个“类 Word 实时编辑”总开关，默认开启；关闭后立即恢复 Obsidian 原生实时预览。
- 只处理当前编辑器事务，不扫描或批量改写 Vault，不触碰 frontmatter、评论、引用和属性数据。
- 所有变更必须进入 CodeMirror 历史，可由一次撤销恢复；插件不得直接改写编辑器 DOM 代替 Markdown 事务。
- KnowGrove 独立实现该规范，不集成 MDRazor 的失联图片清理、目录聚焦、垂直标签、工作区自动保存或状态栏增强。

**验收用例**

1. 在实时预览中分别编辑标题、粗体、斜体、高亮、删除线、行内代码和双链，编辑时不显示对应 Markdown 标记，保存后源文件语法有效。
2. 标题和引用在正文起点按退格只解除一次格式，文字不丢失；空标题和空引用按回车变为普通段落。
3. 用两级和三级无序列表验证：回车保持当前层级；退格与 Shift+Tab 每次只降低一级；Tab 每次只增加一级；父项升降级时已有子项保持相对层级。
4. 用至少三项有序列表验证：中间插入、空项降级、同级合并和层级升降后编号连续且不重复；根级空项按回车退出列表。
5. 点击独立图片块后可以一次退格或删除整块；图片前后空段落也能向图片方向整块删除，并可一次撤销恢复。
6. 在闭合代码块内部编辑时保持原生行为；在代码块下方空段落按 Backspace、上方空段落按 Delete 时删除完整代码块，并可一次撤销；四反引号包裹三反引号内容、波浪线围栏和未闭合围栏均不得误判。
7. 任务列表单行、整行和跨行选中时始终显示复选框而不是 `- [ ]`；非空任务回车后生成新的未完成任务，空任务退格或回车完整退出且不残留 `• [`；嵌套任务逐级升降，任务合并不重复标记；点击复选框可勾选并可撤销，源码模式仍显示原始任务语法。
8. 与评论标记、整块拖动引用和 Obsidian 原生撤销重做同时启用时无冲突、无控制台错误。

#### 6.7.2 附件冗余检测与清理

- 插件维护持久化的“附件引用历史台账”。只有至少被 Markdown、Canvas 或 Base 成功引用过一次、且对应附件仍存在的文件，才有资格进入清理候选；从未被引用的图片、音频、视频、PDF 或其他附件永远不进入自动或手动清理清单。
- 当 Markdown 正文删除图片、录音、视频、PDF 或常用 Office / 电子书附件的最后一处引用时，只更新当前来源文件及其历史引用并弹出一次 KnowGrove 提醒，不因单次编辑重扫全库。删除整篇来源笔记时，KnowGrove 只更新引用历史，不再从 Vault `delete` 事件叠加第二个附件弹窗；笔记和随附附件是否进入回收站统一由 Obsidian 原生的一次删除确认负责。Obsidian 未同步处理而留下的历史失联附件，仍可在用户手动“全库检查”时进入候选。
- 正文中的最后一个可见附件区块被删除、但同一 Markdown 的 YAML 仍保留指向该附件的单值或列表属性时，也立即提醒；只有用户确认清理后，才同步移除完全匹配该附件的属性值并把附件移入回收站。其他属性值和正文保持不变。
- 引用索引覆盖 Markdown、Canvas 文件节点与文本链接、Base 中的显式 Wikilink / Markdown 链接；远程 URL、`file:///` 和普通代码文件不参与清理。
- 对同名附件、无法唯一解析的链接和正在写入的 Canvas 采取保守策略：视为可能仍有引用，不进入可删除名单。
- 插件系统目录、隐藏目录、Skills / 代码依赖目录，以及属性治理或附件设置中明确配置的排除目录，不作为附件清理候选，也不进入手动全库引用扫描；用户应把不希望插件治理的大型资料、依赖或外部知识库放入排除范围。
- “增强功能”可单独配置附件检测排除目录；该目录与属性治理排除目录合并生效，路径边界严格按 Vault 相对目录判断，名称前缀相似的相邻目录不会被误排除。
- 默认识别常见图片、音频、视频、PDF、Office 和电子书；用户可补充 ZIP、PSD 或复合扩展名等附件格式。Markdown、Canvas 和 Base 始终作为引用来源，不能被配置成可删除附件。
- 插件启动只加载已持久化的附件引用历史，不读取笔记正文、不重建全库索引，也不执行每日补扫。后台运行只响应当前文件的真实 Vault 创建、编辑、移动和删除事件；Obsidian 启动阶段仅由 MetadataCache 产生的批量 `changed` 事件不得触发附件扫描。
- 首次升级到引用历史台账时，只把升级当时仍能解析出有效引用的附件写入台账；无法证明曾被使用的既有孤立文件不追溯、不猜测、不报告。
- 设置“增强功能”提供“附件冗余检测”总开关、一个统一的“全库检查”按钮、附件检测排除目录和额外附件格式；关闭总开关后停止当前文件的实时提醒，手动检查仍可使用。全库检查只在用户明确点击或执行命令后运行，必须先过滤排除目录，再以低并发分段读取；界面持续展示已处理数、总数与百分比，并提供“停止附件全库检查”命令，防止长任务独占渲染线程。
- 实时提醒中的刚失联附件默认选中；每日或手动扫描结果默认不选中，用户可逐项选择或全选。
- 候选项同时展示上次引用时间和历史来源笔记，帮助用户判断附件为什么进入清单；删除来源笔记时仍使用 Obsidian 删除事件提供的上一版元数据快照更新台账，但不得据此再打开附件清理弹窗。
- 手动确认删除前按当前内存索引再次核对；附件已恢复引用、已移动或不存在时自动跳过。需要全库级最新结论时，用户先执行“全库检查”，插件不得在删除按钮背后静默重建全库索引。
- 所有确认删除都调用 Obsidian `trashFile` 移入用户配置的回收站，不直接永久删除，不改写正文，不删除 Markdown、Canvas 或 Base。
- 产品边界：附件失联清理本身不搬移附件或来源笔记；附件整理是独立、默认关闭且可预览的能力。KnowGrove 不批量转换链接格式，不自动删除空目录，也不自建附件目录规则，而是沿用 Obsidian 全局附件位置。

**验收用例**

1. 一张图片只被一篇 Markdown 引用，删除引用后出现提醒；撤销恢复引用后执行清理会跳过该图片。
2. 同一附件被两篇 Markdown 引用，删除其中一处或删除其中一篇文档均不提醒；删除最后一篇来源时只出现 Obsidian 原生确认，KnowGrove 不叠加窗口，也不对同一附件发起重复回收操作。
3. Canvas 文件节点或文本节点、Base 显式链接引用的附件不会被列为未引用。
4. PDF、图片、录音和视频可被识别；从未引用的附件，以及 Markdown、Canvas、Base、JSON 和代码文件，默认不作为附件候选。配置 ZIP 或 PSD 后，仅这些新增格式可进入候选，引用来源文件类型仍被拒绝。
5. 独立排除目录中的附件不进入清单；同名前缀但不在该目录边界内的附件仍按正常规则检测。
6. 手动扫描只展示历史使用后失联的附件名称、路径、类型、大小、上次引用时间和历史来源；未选择时不能执行，确认后附件进入 Obsidian 回收站。
7. 插件重载后引用历史台账、设置、评论和引用记录保持完整；重载后删除正文最后引用仍立即提醒，删除整篇来源笔记只出现 Obsidian 原生一次确认，控制台无重复删除错误。
8. 在包含至少 7 万文件、数万 `node_modules` / Skills / 外部知识库文件的大型 Vault 中，插件启动不得发起附件全库索引；启动时 MetadataCache 批量事件不得读取这些文件。用户手动全库检查时先排除这些目录，处理过程可见进度、可取消，侧栏滚动和当前笔记编辑保持响应。

#### 6.7.3 附件与链接一致性管理

- KnowGrove 读取 Obsidian 的全局“新附件默认位置”，不再要求用户在插件内重复配置附件目录；支持 Vault 根目录、笔记同目录、笔记相对子目录和固定 Vault 目录。
- 设置“附件随笔记移动”默认关闭。用户开启后，移动 Markdown 笔记时，只处理由该笔记独占、且原本位于旧笔记目录或其子目录中的附件；固定全局附件目录不发生无意义移动。
- 设置“自动整理附件”默认关闭。关闭时仍可从命令面板或笔记右键菜单打开“整理当前笔记附件”预览；“整理全库附件”始终先显示候选表格，默认不选中任何项目。
- 整理目标发生重名时绝不覆盖或删除已有文件，自动生成带序号的新文件名；文件移动使用 Obsidian `FileManager`，让 Obsidian 同步维护内部链接。
- 同一附件被多篇笔记引用时，默认跳过。用户明确选择“复制独立副本”后，才为当前来源创建副本，并仅把该来源笔记的引用改成新的 Obsidian Wikilink；原共享附件和其他来源不变。
- 用户手动执行“检查附件与链接一致性”时扫描四类问题：无法解析的附件链接、位置不符合全局附件规则的附件、多篇笔记共享的附件、历史使用后失联的附件。结果写入插件管理的 `_KnowGrove/附件与链接检查.md`；重复执行复用已打开的报告页，不持续创建重复标签页。
- 一致性报告把路径写为代码文本而非附件链接，避免报告本身重新保护失联附件；报告只记录问题，不自动修改、移动或删除文件。
- 笔记右键菜单增加“整理此笔记附件”和“检查附件与链接”；命令面板增加当前笔记整理、全库整理和一致性检查入口。
- 附件清理、附件整理和一致性检查共用同一套排除目录与扩展名规则，属性治理排除目录继续合并生效。
- KnowGrove 保持 Obsidian Wikilink 作为内部链接规范，不提供把全库 Wikilink 批量改写为 Markdown 链接的命令；不扫描删除全库空目录，也不删除或覆盖重名附件。
- “增强功能”中的开关必须明确显示“已开启 / 已关闭”；用户点击标题、说明、行内空白或原生开关都能切换，键盘聚焦说明区域后可用 Enter 或空格切换。按钮、下拉框和文本输入仍只执行自身动作，不能误触发同一行开关。

**验收用例**

1. 全局附件位置为 `./assets` 时，手动整理把当前笔记独占附件移动到笔记同级 `assets`，正文引用仍能解析且保持 Wikilink。
2. 移动笔记且“附件随笔记移动”开启时，旧笔记目录内的独占附件跟随到新笔记的全局附件位置；固定附件目录、目录外附件和排除目录不移动。
3. 共享附件在默认“跳过”模式不进入整理候选；切换“复制独立副本”后只新增副本并修改当前来源，另一来源仍引用原附件。
4. 目标目录存在同名文件时生成 `名称 2.ext` 等唯一名称，已有文件内容不变。
5. 全库整理默认不选中，用户未选择时不能执行；执行前文件已移动或目标被占用时安全跳过。
6. 一致性报告能区分断链、唯一同名候选、多个同名候选、位置待整理、共享附件和历史失联附件；不能因为报告而改变引用结果。
7. 插件重载后新增设置、评论、引用和附件历史保持完整；真实移动、复制、报告和排除目录验证无控制台错误。

## 7. 数据与隐私

### 7.1 Vault 内数据

- Markdown 正文与 YAML frontmatter
- 下载的正文图片
- 原始音视频文件
- Whisper 逐字稿与 AI 整理结果
- Base 文件
- 主题、课题、项目、生活空间和创作作品
- 插件 `data.json` 中的设置、评论和引用
- 插件管理的 sidecar 文件

### 7.2 Vault 外数据

- 本机托管运行环境，计划位置：
  - macOS：`~/Library/Application Support/KnowGrove/runtime`
  - Windows：`%LOCALAPPDATA%\\KnowGrove\\runtime`
- Obsidian SecretStorage 中的 API Key
- CLI 自身的登录状态
- 规划中的言序云端账号令牌，进入 Obsidian SecretStorage，不写入 Vault。
- 规划中的云端账号、订单、支付状态、Credit 账本、任务 usage 和费率版本，保存于言序服务端；支付卡号、密码等敏感支付信息由支付平台处理。

### 7.3 外发边界

- 只有执行 AI 功能时，才把受限正文、标题和必要属性发送给用户选定的模型。
- 不发送 SecretStorage、浏览器 Cookie、支付信息或无关个人数据。
- 浏览器扩展不能执行任意本地命令。
- 言序云端 AI 只接收当前任务所需内容，不上传整个 Vault，不默认上传原始音视频。
- 云端业务日志不记录完整正文；只保留任务 ID、内容哈希、模型、Token、Credit、时间和错误类别等对账信息。
- 账户服务、模型网关和支付 Webhook 必须使用 HTTPS；支付 Webhook 必须验签并幂等处理。
- 用户删除云端账号时，应能删除可删除的账户资料和任务元数据；依法或为财务对账必须保留的订单和账本记录应在隐私说明中单独披露。

### 7.4 桌面能力边界

- 直接文件系统访问只用于用户明确选择的 Vault 外音视频，以及存放在系统应用数据目录中的签名 Runtime；普通笔记、属性、引用和附件操作使用 Obsidian Vault / FileManager API。
- 本地模型 CLI、FFmpeg、Whisper 与下载器使用明确的可执行文件和参数数组启动，不拼接或调用命令 Shell；只有用户选择相关模型、解析或自动配置功能时才运行。
- 全库枚举只用于用户主动执行的属性检查、主题索引刷新、知识工作台刷新或附件全库检查；附件历史不得在启动时全量重建，日常附件检查只跟随当前文件的真实变更。
- 剪贴板只在用户点击复制路径、复制链接等明确操作时读取或写入；插件不得后台轮询剪贴板。
- 录音悬浮窗位置只保留在当前 Obsidian 会话内，不再写入浏览器 `localStorage`。

## 8. 运行组件与发布状态

### 8.1 组件清单

Runtime 1.0.x 托管：

- `yt-dlp`
- `ffmpeg`
- `ffprobe`
- `whisper-cli`
- `ggml-small.bin`
- `skill-pack.json`
- `THIRD_PARTY_NOTICES.md`
- 签名后的 `runtime-manifest.json`

首轮平台：

- `darwin-arm64`
- `darwin-x64`
- `win32-x64`

远端发布必须采用“一个清单、多个下载源”的结构：

1. 插件内置稳定清单地址，普通用户不需要填写。
2. 清单包含组件 ID、版本、平台、目标路径、文件大小、SHA-256 和下载地址列表。
3. 每个组件优先使用国内可访问主源，失败后自动尝试 GitHub Releases 备用源。
4. 清单使用 Ed25519 签名；插件内置公钥，只接受签名和字段校验均通过的清单。
5. 下载到临时目录并逐文件校验，全部成功后原子切换当前 Runtime。
6. 已安装的可用本机工具优先复用；缺失组件才从远端补齐。
7. Skill Pack 和模型信息跟随 Runtime 版本发布，提示词可独立升级，但不得静默降低最低插件版本要求。

这里的“远端”专指面向插件用户的公开组件分发服务，不是插件源代码仓库。用户即使只通过手工方式拿到 `main.js`、`manifest.json` 和 `styles.css`，首次启用插件后也会在后台从公开 Runtime 清单完成缺失组件下载、校验、本地部署和可用性检测；设置中的“自动配置”保留为查看进度与手动重试入口。

### 8.2 2.5.x 发布目标

- Runtime 版本：`1.0.1`
- Whisper 模型：`ggml-small.bin`
- 默认模型语言策略：自动识别，不把用户语音上传至 KnowGrove 服务器。
- 插件首次环境检查：桌面端首次启用后自动读取签名清单，优先复用本机已有工具，只在缺少视频或语音能力时后台安装当前平台所需组件。
- 后续启动：已就绪且版本未变化时不重复安装；签名清单出现新版 Runtime 时自动原子升级。
- 自动或手动配置：只安装当前平台所需的四个可执行组件，以及共享模型、Skill Pack 和声明文件。
- 自动配置与手动按钮复用同一项安装任务，避免并发下载；自动整理新文档会等待本次启动的组件检查完成再执行。
- 检测清单成功后，在自动配置入口显示当前平台运行包总大小。
- 下载期间持续显示“已下载 / 总包大小”的真实字节进度，并区分检查环境、下载组件、校验文件和完成配置四个阶段。
- 配置结束后持续显示网页文章解析、视频解析、语音转录和 AI 整理四项可用状态；失败时保留失败阶段和已下载大小，便于重试。
- 下载支持多源顺序重试、超时、断点续传、大小校验和 SHA-256 校验。
- 远端不可用时不破坏已有环境，网页文章解析仍可继续使用。
- 设置中保留重新检测和手动配置入口，但普通用户无需理解组件路径。

### 8.3 当前真实状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 本机浏览器接收服务 | 已实现并实测 | `http://127.0.0.1:47831`，仅当前电脑可访问 |
| 网页内置解析 | 已实现 | 不依赖远程 Runtime 下载 |
| 本机已有视频与语音工具复用 | 已实现 | 自动检测本机现有工具 |
| Runtime 构建脚本 | 已实现 | 本地存在跨平台构建与签名脚本 |
| Runtime Skill Pack | 已实现 | 当前版本 `1.0.2` |
| GitHub Runtime 工作流 | 已运行成功 | Runtime 1.0.1 三个平台构建、签名组装与公开发布均通过，运行记录 `30421360036` |
| Runtime 二进制产物 | 已生成 | Runtime `1.0.1`，包含 macOS Apple Silicon、macOS Intel 和 Windows x64，并更新官方 `yt-dlp` |
| CNB 国内下载源 | Runtime 1.0.1 已发布并完成匿名下载与签名审计 | 公共仓库 `lufie-knowgrove/knowgrove-runtime` 的 `runtime-v1.0.1` 提供新签名清单、校验和与变化组件；其余 SHA-256 完全一致的组件复用 CNB `runtime-v1.0.0` 附件，21 项主下载地址全部位于 `cnb.cool` |
| GitHub Releases 备用源 | 已发布 | 公共仓库 `lufie/KnowGrove-runtime` 的 `runtime-v1.0.1`，17 个附件可匿名下载 |
| 插件默认清单地址 | 已内置双源比较 | 设置值留空时先校验 CNB 1.0.1 与 GitHub 最新签名清单，并使用版本最高的有效 Runtime；同版本时优先使用 CNB |
| 新用户自动下载组件 | 启动自动化已实现，完整下载链路已在 macOS Apple Silicon 实测 | 插件 2.5.3 首次启用后会自动触发；底层链路已从默认双源下载 620,187,419 字节，连接波动后可断点续传，7 个组件全部通过大小和 SHA-256 校验并成功启用；Windows 与 macOS Intel 干净机验收仍待完成 |
| 言序云端 AI | EdgeOne 开发环境已部署，未公开发布 | EdgeOne Makers 项目 `knowgrove-cloud-dev` 已部署到 `https://knowgrove-cloud-dev.edgeone.cool`，当前仍使用平台预览访问保护；Neon 新加坡开发项目、邮箱密码、邮箱 OTP 验证和找回密码已配置，EdgeOne 域名已加入 Neon Auth 可信域名。当前插件仍只支持本地 CLI 和用户自带 API，尚未接入云端账户 |
| Credit 账本与套餐 | EdgeOne + Neon 开发环境实测通过，未接真实支付 | PostgreSQL 迁移已写入 Neon；不可变账本、模拟订单、预留、结算和失败释放接口已完成。远端冒烟测试覆盖健康检查、账户创建、2,800 Credit 入账、重复回调幂等和测试数据清理 |
| 购买与支付 | 规划中，未开发 | 已定义外部托管支付页、订单状态、签名 Webhook、到账、退款和拒付链路；尚未选择或接入支付平台 |

### 8.4 地址说明

- 本机浏览器服务：`http://127.0.0.1:47831`
- 源代码仓库：`https://github.com/lufie/KnowGrove`
- Obsidian 社区插件 ID：`knowgrove`（已收录到官方 `community-plugins.json`；2.8.11 官方标签预扫描已完成且源码 / CSS Warning、Error、Recommendation 均为 0，社区目录当前展示版本仍为 2.8.9，尚未完成人工审核）
- 最新 GitHub 正式插件版本：`2.8.11`，发布地址：`https://github.com/lufie/KnowGrove/releases/tag/2.8.11`
- 最新 Obsidian 本地安装包：`release/言序-KnowGrove-2.5.17-安装包/knowgrove-2.5.17.zip`，SHA-256 `1f64e80ae92efcd9b3e969f63aa7a7bc794405eb60204c6c45c2e94069faa4b6`
- 最新浏览器剪藏扩展本地安装包：`release/言序-KnowGrove-2.5.4-安装包/浏览器剪藏扩展/KnowGrove-Capture-0.3.3.zip`，SHA-256 `02424304d772f1bfd6725c7a10844295dd967e19d5a8d91e380dec7adc9a12fd`
- 本地可分发文件夹：`release/KnowGrove-2.5.0-可分发文件/`
- 2.5.1 CLI 修复包：`release/KnowGrove-2.5.1-CLI修复包/`
- 远程运行包主清单：`https://cnb.cool/lufie-knowgrove/knowgrove-runtime/-/releases/download/runtime-v1.0.1/runtime-manifest.json`
- 国内运行包发布地址：`https://cnb.cool/lufie-knowgrove/knowgrove-runtime/-/releases/tag/runtime-v1.0.1`
- GitHub 备用清单：`https://github.com/lufie/KnowGrove-runtime/releases/latest/download/runtime-manifest.json`
- GitHub Release 备用地址：`https://github.com/lufie/KnowGrove-runtime/releases/tag/runtime-v1.0.1`
- 言序云端开发地址：`https://knowgrove-cloud-dev.edgeone.cool`（EdgeOne 平台预览访问保护，仅用于开发验收，不能作为公开生产地址）

不得把本机地址写成远程服务器地址，也不得在远程清单和产物可访问之前宣称“安装即可自动下载组件”。

### 8.5 Runtime 上线验收标准

只有同时满足以下条件，状态才可改为“已发布”：

1. 三个平台构建全部成功。
2. 每个产物都有 SHA-256。
3. 清单通过 Ed25519 签名，插件内公钥能验签。
4. 国内主下载源可在不使用代理的网络环境访问。
5. GitHub Releases 备用源可访问。
6. 插件默认 `manifestUrl` 指向已发布清单。
7. 全新 macOS Apple Silicon、macOS Intel 和 Windows x64 环境完成安装。
8. 分别用真实网页、公开视频和真实语音完成解析并写入 Vault。
9. 安装失败不会破坏原有 Runtime。
10. 许可证和第三方声明随包发布。
11. 从插件默认清单地址执行一次完整远端安装，且不能依赖开发机既有工具。
12. 主源失败时能自动切换备用源，并产生用户可理解的错误信息。
13. 安装界面能显示总包大小、实时已下载大小、阶段进度和安装后的四项能力状态。

## 9. 非功能需求

### 9.1 性能

- 设置页和侧边栏不得因全库扫描持续阻塞界面。
- 大模型批处理按小批次并发，显示真实进度。
- 候选资料列表不得写入 frontmatter 造成属性面板卡顿。

### 9.2 可靠性

- 采集器先创建文件、后写正文时，不产生重复 frontmatter。
- 用户在处理期间修改正文时，不覆盖用户新内容。
- 批量属性修改前验证预览前置条件。
- 插件生成文件不得覆盖同名的用户手工文件。

### 9.3 兼容性

- Obsidian 最低版本：1.13.0。设置页采用 1.13 的可搜索声明式设置 API；旧版本继续保留已有插件数据，但升级插件前应先升级 Obsidian。
- 插件核心支持 Obsidian 桌面端。
- 浏览器接收、CLI、下载和转录只在桌面端运行。
- Runtime 目标支持 macOS Apple Silicon、macOS Intel 和 Windows x64。

### 9.4 可用性

- 界面语言默认跟随 Obsidian，不增加需要用户维护的独立语言开关。
- 完整基准语言为简体中文、繁体中文与英语；主要设置和核心流程同时支持日语、韩语、德语、法语、西班牙语、巴西葡萄牙语和俄语。
- 尚未经过本地语言审校的新文案优先回退英语，不在非中文界面中静默回退简体中文。
- 用户笔记标题、路径、评论、领域、主题和课题等内容数据绝不自动翻译；切换语言不能改写 frontmatter、Base、Markdown 正文或插件数据。
- 英文及使用大小写的语言采用 sentence case；按钮使用动作动词，设置标题使用用户可理解的名词或行为，不逐字翻译中文产品术语。
- 同层级设置使用一致的原生行样式。
- 默认不使用大面积黑色背景或装饰性高亮。
- hover、选中和当前文档状态使用统一视觉语言。
- 中文品牌只使用“言序”，英文品牌只使用“KnowGrove”，不再展示历史产品名。
- 品牌色主值为 `#F24B3F`，深色交互态为 `#D83F35`，浅色背景为 `#FFF0ED`；语义警告和错误颜色保持独立。

## 10. 关键成功指标

- 新建轻量链接笔记自动识别成功率。
- 网页正文解析成功率和图片保留完整率。
- 视频、语音转录成功率。
- 自动处理后无需人工修正的属性比例。
- 待规范笔记数量变化。
- 收集后进入阅读、主题、课题或作品的转化率。
- 创作材料引用覆盖率和无来源内容发生率。
- 评论和块引用修复成功率。
- 新用户从安装到成功处理第一篇资料的时间。
- 云端 AI 首次任务成功率、从安装到首次云端结果的时间。
- 免费体验到付费转化率、基础版与标准版续费率。
- 每项任务预计 Credit 与实际 Credit 的偏差。
- 支付成功到 Credit 到账的 P50 / P95 时间和重复发放事故数。
- 失败任务自动释放 Credit 的成功率和账本对账差异。
- 单付费用户模型成本、云资源成本、支付成本、毛利润和整体毛利率。
- Flash → Plus → 3.7 Plus 的自动升级率、重试率和用户重新生成率。

## 11. 当前已知缺口

1. macOS Intel 和 Windows x64 已完成云端构建，仍需在干净实体环境完成插件内托管安装。
2. CNB 主清单与组件已发布，但还需从插件默认 CNB 清单执行一次完整的干净环境安装，并验证主源失败时切换 GitHub。
3. Safari 扩展、Chrome 商店分发和新用户安装流程需要独立发布验收。
4. 真实图片生成依赖外部图片模型和密钥，默认关闭。
5. 网络调研和自动下载更多资料尚未形成完整、可审计的正式流程。
6. 言序云端账户、邮箱认证和 Credit 账本已部署到 EdgeOne + Neon 开发环境，并通过远端测试身份的账户、订单和 Credit 幂等冒烟测试；真实邮箱 OTP 全链路、Obsidian 设备配对、模型网关、公开生产域名和真实支付仍未完成。
7. 支付平台、商户主体、自动续费规则、退款政策、税务和用户协议仍需在开发前确认。
8. 80% 毛利为基于当前 Qwen 官方原价和成本假设的目标，必须用首个完整结算周期的真实账单复核，不能作为已验证结果对外宣传。

## 12. 发布与验收流程

每个插件版本至少执行：

1. `git status --short --branch`
2. `git fetch`
3. `npm run check`
4. `npm test`
5. `git diff --check`
6. `npm run build`
7. 只部署 `main.js`、`manifest.json`、`styles.css`
8. 不覆盖用户已有 `data.json`
9. 重载 KnowGrove
10. 使用 Obsidian 实机验证对应页面和真实输入
11. 检查 `obsidian dev:errors`
12. 检查 `obsidian dev:console level=error`

## 13. PRD 维护规范

### 13.1 每次产品改动必须更新

任何用户可感知变化都必须完成以下动作：

1. 修改本 PRD 中对应功能的当前行为。
2. 更新“当前产品版本”。
3. 如状态发生变化，更新“运行组件与发布状态”。
4. 在“变更记录”新增一行。
5. 新功能必须补充验收标准或测试要求。
6. 删除功能时，正文删除对应能力，并在变更记录说明迁移或替代入口。

### 13.2 状态定义

- **规划中**：只有产品方案，尚未实现。
- **开发中**：代码未完成全部测试或未交付。
- **已实现**：代码和自动化测试通过。
- **已部署**：已经安装到目标环境。
- **已实测**：使用真实界面或真实输入完成验收。
- **已发布**：其他用户可以从正式渠道安装并使用。

不得用“已实现”代替“已发布”。

### 13.3 变更记录模板

| 日期 | 产品版本 | PRD 版本 | 变更类型 | 变更内容 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-15 | 2.8.11（GitHub 正式发布，官方标签预扫描完成） | 1.8.5 | 修复 / CSS 兼容性警告治理 / 发布 | 根据 2.8.10 官方分支预扫描的唯一剩余警告，将评论文本的 `text-decoration-*` 下划线实现替换为兼容的内阴影，保留原有标记视觉与交互；版本映射同步更新至 2.8.11 | PR #15 通过 CI 后合并；GitHub Release、三个标准资产和来源证明已发布；官方 2.8.11 标签预扫描状态 `Completed`，源码 Warning 0、CSS Warning 0、Error 0、Recommendation 0；当前 Vault 已覆盖安装并重载，`data.json` 哈希不变，插件错误和控制台错误为 0；社区目录当前展示版本仍为 2.8.9，等待平台自动刷新，Obsidian 员工人工审核仍待完成 |
| 2026-08-15 | 2.8.10（GitHub 正式发布，官方标签预扫描完成） | 1.8.4 | 修复 / 社区源码警告治理 / 发布 | 根据 2.8.9 已完成的社区扫描报告逐项治理 6 类 TypeScript 类型安全源码 Warning 和 2 条 CSS 兼容性 Warning：显式声明 Node 类型环境，将 `no-unsafe-member-access`、`no-unsafe-assignment`、`no-unsafe-call`、`no-unsafe-argument`、`no-unsafe-return` 与 `prefer-promise-reject-errors` 固化为本地 Error 门禁；移除扫描器无法安全推断的 `Array.prototype.at()` 用法并规范子进程错误；拆分文本装饰简写，移除扩展系统字体关键字 | 本地完整门禁、PR #14、GitHub Release、标准资产和来源证明均完成；官方标签预扫描确认 6 类 TypeScript 源码 Warning 全部清零，仅剩 `text-decoration-*` CSS 兼容性 Warning，随后由 2.8.11 清理。文件系统访问与受控本地进程属于媒体导入、录音恢复、Runtime 和用户选择 CLI 的核心能力，显式全库检查与剪贴板操作均由用户动作触发，继续作为准确能力披露保留 |
| 2026-08-15 | 2.8.9（GitHub 正式发布，社区扫描完成） | 1.8.3 | 修复 / 社区警告治理 / 发布 | 启用 `eslint-plugin-obsidianmd` 完整推荐规则与 TypeScript 类型规则并把警告视为失败；移除不安全未知值转换、遗留 `require`、跨窗口 `instanceof`、异步事件误用、硬编码配置目录、废弃 Workspace / Slider API 和浏览器 `localStorage`；设置页迁移到 Obsidian 1.13 可搜索声明式 API，最低版本调整为 1.13.0；清理插件、浏览器扩展和云端样式中的 `!important`、扩展系统字体与过时文本装饰写法；补充文件系统、本地进程、全库枚举和剪贴板的主动触发边界；新增 PR 与 `main` 推送自动验证工作流，并将 GitHub Actions 升级到 Node 24 运行版本 | 官方规则 lint 达到 0 错误 / 0 警告，类型检查、235 项测试、生产构建与差异检查全部通过；PR #11 合并并发布 2.8.9，标准资产与来源证明齐全；当前 Vault 已覆盖安装并重载，`data.json` 哈希保持不变，插件及控制台错误为 0；PR #12 完成 CI Actions 升级且流水线通过；社区后台已完成 2.8.9 / `3b897a5` 扫描，发布资产、网络、依赖和可复现构建均通过，0 个阻断 Error；报告中的源码与 CSS 警告转入 2.8.10 修复 |
| 2026-08-14 | 2.8.8（正式发布，社区自动复检完成） | 1.8.1 | 修复 / 发布 / 社区自动审核 | 从 Obsidian 社区作者后台逐项核对 2.8.4 报告，确认唯一阻断 Error 为录音悬浮窗对 `right` / `bottom` 的直接静态样式赋值；改为 Obsidian `setCssProps`，并将 `eslint-plugin-obsidianmd` 0.4.1 的全部官方 Obsidian 规则及其严重级别同步进本地 Lint 门禁；移除两处非必要 `console.info`，避免新版规则把开发诊断日志升级为发布阻断 | 修改以独立 Git 提交保存并通过 PR #9 合并；官方 Obsidian 规则门禁、类型检查、235 项单元测试、生产构建和差异检查全部通过；2.8.8 已覆盖部署并重载到当前 Documents Vault，`data.json` 哈希不变，插件与控制台错误为 0；GitHub Release `2.8.8`、三个标准资产及构建来源证明已发布；社区后台识别版本 2.8.8 / 提交 `0681658`，自动复检状态为 `Completed`、7 项 Pass、29 项 Warning、14 项 Recommendation、0 个阻断 Error，市场页恢复展示 `Add to Obsidian` 安装入口；人工审核与 Windows/macOS Intel 干净机验收仍待完成 |
| 2026-08-14 | 2.8.7（本地验证候选） | 1.7.9 | 新增 / 本地音视频导入 | 录音工作区新增本地音视频导入入口，支持拖拽或点击多选，并明确展示 8 种音频与 4 种视频格式；外部大文件通过本地文件系统复制到 Vault，库内文件直接复用，重名不覆盖；每个媒体生成独立 Wikilink 笔记并复用 Whisper、系统语言分析和后台任务链路，结果行持续展示状态并提供打开笔记入口；空闲和录音完成状态均可导入，录音进行中不打断当前录制 | lint、类型检查、235 项单元测试、生产构建和差异检查全部通过；2.8.7 已覆盖部署并重载到当前 Documents Vault，安装文件与构建产物哈希一致，部署未覆盖 `data.json`；重载后 29 项设置、4 条评论/引用和 644 条附件历史完整；实际完成态 DOM 验证导入区、多选及 12 种格式限制生效，不写入 Vault 的拖拽事件模拟成功显示“解析完成”和打开笔记入口，模拟产物已清除；Obsidian 未捕获 KnowGrove 插件错误；真实大文件复制、转录耗时与结果仍待用户手动验收，2.8.7 尚未公开发布 |
| 2026-08-14 | 2.8.6（本地验证候选） | 1.7.8 | 新增 / 本地音视频语言策略 | 本地语音与视频统一进入媒体转录链路，补充 `mp4`、`mov`、`mkv`、`m4v` 视频引用识别；Whisper 自动识别实际说话语言，逐字稿和原文引用保持原语言，AI 摘要、要点、整理正文及分析章节标题统一使用本地系统语言；既有属性键、Frontmatter 与原始媒体 Wikilink 不改写 | 类型检查、lint、233 项单元测试、生产构建和差异检查通过；2.8.6 已覆盖安装并重载到当前 Documents Vault，程序文件与构建产物哈希一致；覆盖程序文件本身未改写 `data.json`，重载后的插件正常保存运行态数据，29 项设置、4 条评论/引用和 644 条附件历史完整保留；运行环境识别为 `zh-CN`，使用英文原文和本机运行时替身验证 Prompt 明确要求中文分析并禁止翻译原句，返回正文保留英文原句且摘要与标题为中文；插件与控制台错误为 0；未擅自采集或处理用户真实音视频，真实材料效果待用户验收，2.8.6 尚未公开发布 |
| 2026-08-12 | 2.8.5（本地验证候选） | 1.7.7 | 性能优化 / Mac 录音保存 | 将录音停止流程拆为即时保存与后台整理两段：音频封口、基础属性和 Markdown 笔记写入后立即完成，不再同步等待 AI 语义属性、最长 5 秒附件索引或转录整理；后台任务继续按原顺序完成属性和内容解析并独立报告错误；单片段保存文案不再误报为“正在合并”；增加保存阶段耗时诊断日志 | 类型检查、lint、230 项单元测试、生产构建和差异检查通过；2.8.5 已覆盖安装并重载到当前 Documents Vault，`data.json` 覆盖前后哈希一致；运行时将后台属性处理替换为永不返回的测试桩时，录音笔记仍在 92 ms 内创建并完成前台保存，证明保存不等待后台处理；测试笔记、音频和测试台账已清理，4 条评论/引用保留，插件与控制台错误为 0；真实麦克风停止体感仍由用户实测，2.8.5 尚未公开发布 |
| 2026-08-12 | 2.8.4（正式发布） | 1.7.6 | 修复 / 附件删除确认 / 发布 | 修复删除带录音等附件的整篇笔记时，Obsidian 原生删除确认与 KnowGrove“附件引用已移除”弹窗叠加的问题：整篇来源笔记删除统一交给 Obsidian 的一次确认，KnowGrove 只更新历史台账；正文单独移除最后引用时仍保留附件清理提醒；避免两个流程争抢回收同一附件导致 `ENOENT` | 230 项单元测试、类型检查、lint、生产构建和差异检查通过；2.8.4 已覆盖安装并重载到当前 Documents Vault，可回收测试验证删除带独占录音附件的整篇笔记时仅出现 Obsidian 原生一个确认，KnowGrove 弹窗为 0，附件只回收一次；测试产物与台账记录已清理，4 条评论/引用及 643 条附件历史保持完整；GitHub Release `2.8.4` 的三个标准资产和构建来源证明已发布，公开资产哈希分别为 `main.js f5bb4721…6c48`、`manifest.json befe16fe…a799`、`styles.css 193ada6d…4301`，使用公开资产回装后 `data.json` 哈希不变，插件与控制台错误为 0；发布地址为 `https://github.com/lufie/KnowGrove/releases/tag/2.8.4`，已进入 Obsidian 社区插件自动更新链路 |
| 2026-08-12 | 2.8.3（正式发布） | 1.7.5 | 优化 / Mac 录音交互与性能 / 发布 | 链接解析保持弹窗；录音恢复为工作台下方可关闭的临时左侧标签，录音页取消手动收起按钮，录制中切换或关闭时自动出现可拖动悬浮窗，返回录音页自动隐藏；悬浮窗恢复入口改为“返回录音”并记住拖动位置；单片段停止后直接保存原编码，多段同格式优先流复制合并，避免默认 AAC 转码造成长时间等待 | 类型检查、lint、229 项单元测试、生产构建和差异检查通过；GitHub Release `2.8.3`、三个标准安装资产和构建来源证明已发布，公开资产哈希与当前 Documents Vault 安装完全一致；使用公开发布资产覆盖安装并重载后 `data.json` 哈希保持不变，实机确认录音只打开一个左侧临时标签且无 Modal，悬浮窗切换、关闭、拖动、位置记忆和“返回录音”均通过，插件及控制台错误为 0；发布地址为 `https://github.com/lufie/KnowGrove/releases/tag/2.8.3`，已进入 Obsidian 社区插件自动更新链路；未擅自采集环境声音，真实麦克风停止耗时与电话接力恢复仍待用户手动验收 |
| 2026-08-12 | 2.8.2（正式发布） | 1.7.3 | 优化 / 弹窗交互 / 发布 | “批量存链接”和“录音”保留为两个独立 Ribbon，但由侧边栏 ItemView 改为轻量 Modal；点击后只显示操作弹窗，不再向左侧标签索引追加条目；插件加载时清理 2.8.0–2.8.1 遗留的收集、存链接和录音视图，录音中关闭弹窗仍转为可恢复的悬浮录制框 | 类型检查、lint、229 项单元测试、生产构建和差异检查通过；GitHub Release `2.8.2`、三个标准安装资产和构建来源证明已发布，公开资产哈希与本地构建、当前 Documents Vault 安装完全一致；使用公开发布资产覆盖安装并重载后 `data.json` 哈希保持不变，插件及控制台错误为 0；发布地址为 `https://github.com/lufie/KnowGrove/releases/tag/2.8.2`，已进入 Obsidian 社区插件自动更新链路；未擅自采集环境声音，真实麦克风、悬浮录制和电话接力恢复仍待用户手动验收 |
| 2026-08-12 | 2.8.1（本地候选） | 1.7.1 | 优化 / Mac 链接存储与录音 | 将统一“收集”页拆成“批量存链接”和“录音”两个独立 Ribbon 与独立侧边栏视图；移除重复标题、副标题和页内标签，精简录音与链接提示；批量链接完成后逐篇提供可随文件重命名更新的“打开对应笔记”入口，录音完成后继续提供“打开录音笔记”；旧 2.8.0 收集视图在加载时迁移为链接存储视图 | 类型检查、lint、229 项单元测试、生产构建和差异检查通过；2.8.1 已覆盖安装并重载到当前 Documents Vault，实机确认两个独立 Ribbon、两个独立视图、无重复标题或页内标签；使用不写入 Vault 的运行时模拟任务验证链接解析结果会逐篇显示可打开笔记入口；旧收集视图迁移及重复视图收敛通过；部署前后 `data.json` 哈希一致，4 条评论/引用、644 条附件历史和 29 项设置完整，Obsidian 插件及控制台错误为 0；未擅自采集环境声音，真实麦克风、悬浮录制和电话接力恢复仍待用户手动验收，尚未公开发布 |
| 2026-08-11 | 2.8.0（本地候选） | 1.7.0 | 新增 / Mac 收集与安全录音 | 左侧增加统一“收集”入口：批量粘贴单篇、多篇或混合文字中的链接，每个唯一链接创建一篇 Markdown 并进入既有解析队列；增加可收起为悬浮框的 Mac 录音页，按 2 秒数据块同步落盘、10 分钟安全分段、原子会话清单和异常退出恢复；电话接力或其他应用占用麦克风时先快速重试，再低频等待系统归还并续接同一会话；批量链接和录音目录可分别配置并默认沿用收集箱；本地语音扩展支持 `webm`，长音频优先直接交给 FFmpeg/Whisper，避免完整读入渲染进程内存 | 类型检查、lint、229 项单元测试、生产构建和差异检查通过；2.8.0 已覆盖安装并重载到当前 Documents Vault，新增命令、单一 Ribbon、两个标签页、两项路径设置和 Electron 音频格式支持完成实机检查；一次可回收批量链接实测正确生成独立笔记、基础属性与唯一来源链接，测试笔记已移入回收站；`data.json` 迁移后保留 4 条评论/引用和 643 条附件历史，Obsidian 插件及控制台错误为 0；未擅自采集环境声音，真实麦克风、悬浮录制和电话接力恢复等待用户手动验收，尚未公开发布 |
| 2026-08-11 | 2.7.6（正式发布） | 1.6.8 | 修复 / 类 Word 编辑 / 发布 | 将任务列表的回车、退格、Delete 和层级移动纳入类 Word 原子编辑规则：复选框与列表标记不可拆分；非空项回车创建同层级未完成任务；空根级任务一次退出，嵌套任务逐级退出；任务合并、缩进和有序任务重排保持标准 Markdown；对历史遗留的独立 `- [` 残缺标记提供一次退格清理 | 类型检查、lint、224 项单元测试和生产构建通过；2.7.6 已覆盖部署并重载到当前 Documents Vault，程序文件覆盖前后 `data.json` 哈希一致；临时真实编辑器中从 `- [ ] 苟下去就是第一生产力` 回车生成新任务，再退格后编辑器精确恢复为空行，且独立 `- [` 可一次退格清空；无 `• [`、半截任务标记或控制台错误，测试笔记已清理；GitHub Release `2.7.6` 的三个安装资产已发布且与本地构建、当前安装哈希一致，已进入 Obsidian 社区插件自动更新链路 |
| 2026-08-11 | 2.7.5（正式发布） | 1.6.7 | 修复 / 类 Word 编辑 / 发布 | 实时预览中的任务列表改为由类 Word 编辑层持续渲染单一可交互复选框；整行或跨行选中时不再暴露 `- [ ]` / `- [x]` Markdown 标记，点击仍只改写标准任务状态；任务列表回车、缩进和删除语义继续沿用 Obsidian 原生行为，源码模式不隐藏原始语法 | 类型检查、lint、223 项单元测试和生产构建通过；2.7.5 已部署并重载到当前 Documents Vault，覆盖安装前后 `data.json` 哈希一致，重载后原有 28 项设置与 4 条评论/引用仍完整且无控制台错误；在真实笔记 `Home/🌟行动/2026年7月第4周.md` 的截图对应任务上验证整行选中只显示一个复选框和正文；临时笔记点击后由 `- [ ]` 精确变为 `- [x]`，测试产物已清理；GitHub Release `2.7.5`、三个安装资产和构建来源证明已发布，资产哈希与当前安装一致，已进入 Obsidian 社区插件自动更新链路 |
| 2026-08-03 | 2.7.4（市场说明修复） | 1.6.6 | 修复 / 国际化 / 市场展示 | 将根 README 与各语言 README 的语言导航从仓库相对路径改为完整 GitHub HTTPS 地址，避免 Obsidian 社区插件详情页缺少仓库路径上下文时把语言链接误判为 Vault 内部路径；明确市场语言入口用于打开对应语言说明，插件界面语言仍自动跟随 Obsidian | 10 个支持语言的导航链接均已改为可独立解析的公开 HTTPS 地址；不修改插件代码、版本或用户数据，市场页重新读取默认分支 README 后生效 |
| 2026-08-03 | 2.7.4（正式发布，社区目录已收录） | 1.6.5 | 修复 / 大库性能 / 发布 | 附件治理改为启动零扫描：仅加载持久化引用历史，忽略启动期 MetadataCache 批量事件；日常只增量处理用户刚创建、编辑、移动或删除的当前来源文件。全库检查改为唯一手动入口，扫描前过滤插件、隐藏、Skills、依赖和用户排除目录，按小批次主动让出渲染线程，显示处理数量与百分比并支持命令取消；附件解析候选由逐附件线性搜索改为路径、文件名和主干名索引；台账拆分当前来源与历史来源，一致性报告复用已有标签页 | 类型检查、lint、222 项单元测试与生产构建通过；当前约 7.7 万文件 Vault 已部署并重载 2.7.4，确认启动及重载 `fullScan=false`、无控制台错误；手动全库检查只纳入 1,086 个治理来源并可成功取消；真实当前笔记增量验证通过，4 条评论/引用和 612 条附件历史保持完整。GitHub Release `2.7.4` 及三个安装资产已发布，自动发布流程与来源证明通过，资产哈希与本地部署一致；插件 ID `knowgrove` 已收录到 Obsidian 官方社区插件清单，当前仍标记为尚未由 Obsidian 员工人工审核 |
| 2026-08-02 | 2.7.3（正式发布，社区自动审核完成） | 1.6.3 | 修复 / 发布合规 | 根据社区审核页的真实失败报告，修复 3 个阻断错误：最低 Obsidian 版本提升到实际 API 下限 1.11.4，并将依赖本地 CLI、服务与运行组件的插件声明为桌面专属；Markdown 回退渲染改用独立 `MarkdownRenderChild` 管理生命周期；设置页标题统一改用 `Setting.setHeading()`。新增官方 `eslint-plugin-obsidianmd` 发布前门禁，并为 GitHub Release 的 `main.js`、`manifest.json`、`styles.css` 生成构建来源证明 | 官方规则预检、类型检查、221 项单元测试、生产构建和差异检查全部通过；当前 Vault 已部署并重载 2.7.3，三个安装文件与构建/线上 Release 哈希一致，`data.json` 哈希、4 条评论/引用和 612 条附件历史保持不变；GitHub Release 流水线与来源证明通过，社区重新扫描为 Completed、0 个阻断 Error，构建复现、网络和依赖检查通过；后续人工审核待完成 |
| 2026-08-02 | 2.7.2（本地验证候选） | 1.6.2 | 修复 / 设置交互 | 合并附件冗余检测旁功能重叠的“立即检查 / 全面检查”为一个“检查附件”入口；点击后立即显示检查中并禁用按钮，统一执行断链、位置、共享与历史失联扫描，完成时打开报告，组件未就绪或执行失败时给出明确提示 | 类型检查、221 项单元测试、生产构建与差异检查通过；当前 Vault 已部署并重载 2.7.2，安装文件与构建产物哈希一致；实机生成统一报告（断链 50、位置待整理 25、共享附件 6、历史失联 0），Obsidian 未报告控制台错误；`data.json` 哈希、4 条评论/引用和 612 条附件历史保持不变 |
| 2026-08-02 | 2.5.20（开发中） | 1.4.18 | 优化 / 主题索引 | 在“增强功能”增加默认开启的主题列表总开关；取消按首个领域强制分组，改为全部主题平铺；主题工作区可选设置“上级主题”，只有显式关系才缩进，清空后恢复顶层，并保护循环、重命名和删除关系 | 212 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.20，实测关闭后主题视图归零且 Ribbon 隐藏，重新开启后恢复单一视图；当前 80 个首屏主题无领域分组；临时父子主题实测深度为 0/1，移除 `上级主题` 后均回到深度 0，测试笔记已移入回收站；4 条评论/引用和 611 条附件历史保留，Obsidian 无插件或控制台错误 |
| 2026-08-02 | 2.5.19（开发中） | 1.4.17 | 修复 / 附件治理 | 将扫描口径由“当前未引用”改为“历史曾引用、当前失去最后引用”；持久化引用历史、正文来源与最后来源，防止从未使用的素材被误列；正文引用删除但同文档 YAML 仍有残留链接时立即提醒，确认后同步清除精确属性链接；覆盖插件重载、索引未完成和来源文档删除 | 210 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.19；旧算法的 166 个候选归零，手动扫描提示“没有发现历史使用后失联的附件”；真实临时音频验证重载后删除正文区块立即提醒、确认后精确删除 YAML 音频属性并回收附件，从未引用图片保持原位且不进清单；真实临时 PDF 验证删除来源文档立即提醒；测试产物已清理，设置与 4 条评论/引用记录保持完整 |
| 2026-08-02 | 2.5.18（开发中） | 1.4.16 | 新增 / 附件治理 | 增加默认开启的附件冗余检测：Markdown 删除引用或来源文档删除后，仅在附件失去全库最后一处引用时提醒；每天干跑检查一次未引用附件；Markdown、Canvas、Base 统一参与引用核对，同名与模糊路径保守保留；用户确认后只移入 Obsidian 回收站 | 210 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.18；约 7.6 万文件的手动扫描约 3 秒完成且只出现一个清单，治理排除目录生效；真实临时 PDF、共享图片和 Canvas 图片验证“最后一处引用消失才提醒”、多处引用不误报、Canvas 引用不误报，删除均经 Obsidian 回收站；测试产物已清理 |
| 2026-08-02 | 2.5.17（开发中） | 1.4.15 | 本地交付 | 生成当前完整代码对应的标准 Obsidian 本地安装包 `release/言序-KnowGrove-2.5.17-安装包/knowgrove-2.5.17.zip`，不包含用户设置、评论、引用或其他 `data.json` 数据 | 205 项单元测试、类型检查和生产构建通过；压缩包仅包含 `knowgrove/main.js`、`manifest.json`、`styles.css`，内置 Manifest 版本为 2.5.17，SHA-256 为 `1f64e80ae92efcd9b3e969f63aa7a7bc794405eb60204c6c45c2e94069faa4b6`；这是本地候选包，不等同于正式公开发布 |
| 2026-08-02 | 2.5.17（开发中） | 1.4.14 | 优化 / 编辑体验 | 围栏代码块增加与媒体块一致的块外删除语义：代码块内部完全保留原生编辑；前方空段落按 Delete 或后方空段落按 Backspace 时一次删除完整闭合代码块；支持反引号、波浪线和不同围栏长度匹配，未闭合围栏不自动处理 | 205 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.17，真实编辑器验证块后 Backspace、块前 Delete 均只留下一个空段落且可一次撤销，块内按键保持单字符原生编辑，data.json 哈希不变，Obsidian 无插件或控制台错误，测试笔记已清理 |
| 2026-08-02 | 2.5.16（开发中） | 1.4.13 | 修复 / 编辑体验 | 类 Word 实时编辑明确接管无序与有序列表层级：回车同级续项、空项逐级退出、正文起点退格逐级降级、Tab / Shift+Tab 升降一级、父项携带子树移动、Delete 合并同级项，并在插入、合并和层级变更后自动重排有序编号；任务列表仍交给 Obsidian 原生行为 | 200 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.16，真实编辑器验证三级退格、Tab / Shift+Tab 往返、空项两步退出、有序列表中间插入与 Delete 合并后的连续编号均符合规范，操作可一次撤销，用户 data.json 哈希不变，Obsidian 无插件或控制台错误，测试笔记已清理 |
| YYYY-MM-DD | x.y.z | x.y.z | 新增 / 调整 / 删除 / 修复 | 简述行为和入口变化 | 已实现 / 已部署 / 已实测 / 已发布 |

## 14. 变更记录

| 日期 | 产品版本 | PRD 版本 | 变更类型 | 变更内容 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-02 | 2.7.1（本地验证候选） | 1.6.1 | 优化 / 设置交互 | “增强功能”全部开关增加明确的“已开启 / 已关闭”状态；标题、说明与行内空白支持点击切换，并补充 Enter / 空格键盘操作；按钮、下拉框、输入框与原生开关继续阻止行级重复触发 | 类型检查、221 项单元测试和生产构建通过；当前 Vault 已部署 2.7.1，插件入口和功能视图已恢复运行，安装清单及 4 条评论/引用、612 条附件历史保持完整；设置行点击、键盘切换与控件阻断逻辑完成代码验收，Obsidian 1.13.4 当前窗口因持续高负载导致设置模态无法稳定自动打开，尚未将新视觉描述为完整实机验收或正式发布 |
| 2026-08-02 | 2.7.0（本地验证候选） | 1.6.0 | 新增 / 附件与链接治理 | 对齐 Consistent Attachments and Links 的有效能力：沿用 Obsidian 全局附件位置，增加附件随笔记移动、自动或手动附件归集、共享附件跳过/复制策略、重名安全改名、全库一致性报告、当前笔记/全库预览入口及统一排除范围；保留 KnowGrove 的历史引用台账、最后引用确认、Canvas/Base 保护和回收站清理，并明确不批量改写 Wikilink、不扫删全库空目录、不覆盖重名文件 | 类型检查、221 项单元测试和生产构建通过；当前 Vault 已部署并重载 2.7.0，实测预览弹窗、`./assets` 归集、附件随笔记跨目录移动、共享附件默认跳过/可选复制、同名自动使用 `名称 2.ext` 且原文件不变；一致性报告识别 50 个断链、25 个位置待整理、6 个共享附件和 0 个历史失联项且未自动修改；测试产物已移入 Obsidian 回收站，4 条评论/引用记录保留，插件与控制台错误均为 0；这是本地候选，未替换 2.6.1 社区审核版本 |
| 2026-08-02 | 2.6.2（本地验证候选） | 1.5.2 | 修复 / 附件治理 | 对照 Consistent Attachments and Links 校验附件失联清理：新增独立附件排除目录、可配置额外附件格式、来源笔记删除前的元数据快照，以及候选项的上次引用时间和历史来源证据；继续坚持“历史曾被引用且失去全库最后引用”才进入候选，明确不自动搬移附件、批量改写链接或删除空目录 | 219 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.6.2，真实共享 PDF 验证删除第一处引用不提醒、删除最后一处才提醒，真实来源笔记删除也立即提醒，清单正确展示历史来源与上次引用；测试产物已移入回收站，4 条评论/引用记录保留，Obsidian 插件错误和控制台错误均为 0；这是本地候选，2.6.1 社区审核版本未被替换 |
| 2026-08-02 | 2.6.1（社区目录公开，自动审核中） | 1.5.1 | 修复 / 发布合规 | 根据 Obsidian 官方 Manifest 命名规则，将市场显示名称从包含中文与特殊分隔符的“言序 · KnowGrove”调整为 Basic Latin 名称“KnowGrove”；中文界面与中文 README 继续使用“言序”，内部 ID 保持 `knowgrove`，不迁移或改写用户数据 | 类型检查、217 项测试、生产构建、GitHub Release 工作流和本机 Obsidian 重载均通过；线上安装资产完整，插件名称、版本和中文语言环境正确，控制台无错误，`data.json` 校验和部署前后保持一致；社区公开条目 `https://community.obsidian.md/plugins/knowgrove` 已创建并识别版本 2.6.1 / 提交 `c9453e0`，官方自动审核仍在运行，尚未声明审核通过 |
| 2026-08-02 | 2.6.0（公开候选） | 1.5.0 | 新增 / 国际化 / 开源发布 | 建立统一 UI 语言目录，自动跟随 Obsidian 语言；支持简体中文、繁体中文、英语、日语、韩语、德语、法语、西班牙语、巴西葡萄牙语和俄语，用户标题、路径、主题、评论等内容不参与翻译；为全部支持语言增加独立 GitHub README；补充社区插件上架方案、隐私与安全说明、GitHub Release 自动构建流程；浏览器任务文件改用 `Vault.configDir`，不再硬编码 `.obsidian`；源代码仓库改为公开并发布正式 `2.6.0` Release | 类型检查、217 项插件测试、4 项云端测试、插件与云端生产构建、浏览器扩展安全检查和 GitHub Release 工作流均通过；线上安装资产 SHA-256 与本地构建一致；当前 Vault 已完成英文与简体中文实机检查，插件 2.6.0、错误日志及用户数据正常；Obsidian 社区目录尚待官网登录提交与审核，不得描述为市场已上架 |
| 2026-08-02 | 2.5.15（开发中） | 1.4.12 | 新增 / 编辑体验 | 增加默认开启的类 Word 实时编辑：在实时预览中持续隐藏常用 Markdown 标记，标题、列表与引用支持格式优先的回车/退格语义，独立图片和媒体引用支持整块选择、相邻空段落整块删除；源码模式及复杂语法保留原生编辑，设置中提供总开关 | 191 项单元测试、类型检查和生产构建通过；当前 Vault 已部署 2.5.15，实测常用格式标记持续隐藏、图片正常渲染、相邻空段落整块删除与一次撤销恢复、源码模式完整显示原始语法；MDRazor 保留安装但已停用，4 条评论/引用记录保留，测试笔记已移入回收站，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.14（开发中） | 1.4.11 | 优化 / 批量编辑 | 主题索引改为一次建表与缓存复用，领域按需展开并对主题、关联文档渐进加载；增加列表与详情留白；主题详情增加批量重命名和删除流程，重命名同步全部关联笔记，删除只移除主题属性并将正式主题工作区移入 Obsidian 回收站 | 183 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.14，初始主题 DOM 从 2644 行降为 41 行，拥有 1297 个主题的领域只渲染首批 80 行，48 篇关联资料的主题只渲染首批 40 篇，详情同步打开耗时约 5.2 ms；临时笔记实测批量改名与删除均只修改目标主题，保留另一主题和正文，测试产物已移入回收站；4 条评论/引用记录保留，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.13（开发中） | 1.4.10 | 新增 / 主题索引 | 新增独立“主题”侧栏入口，直接列出全部笔记主题属性并按领域分组；不再使用工作台三篇聚类门槛；支持按主题、领域、文档标题和路径搜索，主题详情展示真实关联文档并区分正式知识树主题与原始主题 | 181 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.13，确认只保留一个主题视图；实测可检出仅关联 1 篇笔记的“金融常识”，详情正确显示真实文档与“尚未进入知识树”；4 条评论/引用记录保留，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.12（开发中） | 1.4.9 | 修复 / 界面调整 | “最近”分类改用 Obsidian 原生文件夹的 `right-triangle` 箭头及折叠状态，点击时只切换原生状态，不再重建列表；标题位置、箭头方向和旋转动效与 `_KnowGrove`、`Home` 保持一致 | 178 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.12，实测“最近”与 `Home` 标题起点均为 84 px，展开/折叠使用同一原生箭头且子项正确显示/隐藏；4 条评论/引用记录保留，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.11（开发中） | 1.4.8 | 优化 / 设置 | 调整原生文件列表“最近”虚拟分类的缩进和背景，使其与 `_KnowGrove`、`Home` 根目录同级；在“增强功能”增加最近文件依据和数量设置，支持最近操作、最近编辑、最近新建三种排序以及 3–20 篇显示范围 | 178 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.11，实测“最近”与 `Home` 标题起点均为 84 px，三种依据和 3/4/8 条数量切换即时生效并恢复默认最近操作 8 条；虚拟条目右键菜单与侧栏增强脚本兼容，4 条评论/引用记录保留，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.10（开发中） | 1.4.7 | 新增 / 界面调整 | 确认截图中的“新标签页”为 Obsidian 左侧栏原生新增按钮，并在左侧隐藏该按钮、关闭已有空白侧栏页，主编辑区新建标签能力不受影响；不新增 KnowGrove 侧栏标签，在原生文件列表顶部增加可折叠的“最近”虚拟分类，展示最近打开的 8 篇 Markdown、Base 或 Canvas，过滤附件且不创建真实文件夹 | 178 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.10，左侧新增按钮隐藏而主区按钮保留，最近分类折叠/展开与文件树重绘恢复通过，未创建真实目录，4 条评论/引用记录保留，Obsidian 无插件或控制台错误 |
| 2026-08-01 | 2.5.9（开发中） | 1.4.6 | 删除 / 调整 | 删除 KnowGrove 自建收藏能力：移除设置开关与属性名、阅读列表星标、命令面板与文件右键菜单动作、外部资料自动补齐和属性检查规则；工作流 Base 的“输入与收藏”改为“输入队列”；历史笔记已有 `收藏` 或 `重点关注` 字段作为普通未知属性保留，不批量删除或改写 | 176 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.9，旧收藏设置键完成迁移，4 条评论/引用记录校验值不变，16 篇含 `收藏` 与 674 篇含 `重点关注` 的历史笔记数量不变；工作流 Base 已再生成，阅读列表实机确认仅保留阅读状态与更多菜单，Obsidian 无插件或控制台错误 |
| 2026-07-31 | 2.5.8（开发中） | 1.4.5 | 修复 / 调整 | 自动整理新增本地语音笔记识别：收集箱笔记只有一个本地音频 Wikilink 与少量模板文字时，直接读取现有 Vault 附件并进入 FFmpeg 兼容转换、Whisper 与 AI 整理；监听文件后续修改，支持录音开始时先建笔记、数小时后才写入音频的采集方式；不复制原音频、不写入虚假来源或 `file:///` 链接，整理后保留原音频 Wikilink | 177 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.8，指定 43:58 本地语音完成转录、摘要、要点、对话整理和逐字稿写回；原 71 MB 音频仅保留一份，`data.json` 部署前后哈希一致，Obsidian 无插件或控制台错误 |
| 2026-07-31 | 后续版本（开发中） | 1.4.4 | 新增 / 云端开发部署 | 将言序云端账户和 Credit 服务部署到 EdgeOne Makers 项目 `knowgrove-cloud-dev`；为 EdgeOne Node.js 运行时补充 Neon WebSocket 适配；配置开发环境变量、CORS 和 Neon Auth 可信域名；保留平台预览访问保护，不声明公开生产发布 | EdgeOne 部署 `dpgx7ul2uaye` 构建成功；远端健康检查返回开发模式；远端隔离测试账户验证初始余额 0、标准套餐入账 2,800 Credit、重复完成回调余额保持 2,800，测试数据已清理；真实邮箱 OTP、插件设备配对、模型调用与真实支付尚待验收 |
| 2026-07-31 | 后续版本（开发中） | 1.4.3 | 新增 / 云端开发环境 | 创建 Neon 新加坡开发项目并启用 Neon Auth；邮箱密码、邮箱 OTP 验证和密码找回配置完成；写入 Credit 账本 Schema；增加言序主题的登录、注册、OTP、找回密码和账户余额页面，以及可重复运行的数据库迁移、真实数据库冒烟测试和 EdgeOne 预构建产物 | 4 项 Credit 不变量测试、构建与语法检查通过；真实 Neon 冒烟测试验证账户、模拟订单、2,800 Credit 入账和重复回调幂等；Playwright 验证登录/注册页面与跨域会话检查无控制台错误。EdgeOne 浏览器授权回调异常，尚未获得部署地址，不能描述为已上线 |
| 2026-07-30 | 后续版本（规划中） | 1.4.2 | 新增 / 邮箱账号与云端原型 | 云端账号收敛为仅邮箱注册登录，补充邮箱验证、密码重置、防账号枚举、验证码限流、会话撤销和 Obsidian 设备码配对；新增 `cloud/` EdgeOne Cloud Functions 原型、PostgreSQL Schema、Credit 不可变账本、模拟订单、任务预留/结算/释放接口 | 云服务语法检查和 4 项 Credit 不变量测试通过；尚未登录并创建 Neon 项目，未完成真实邮箱流程、数据库集成或 EdgeOne 部署，不得描述为已上线 |
| 2026-07-30 | 后续版本（规划中） | 1.4.1 | 调整 / 云服务架构 | 首期云服务确定为 EdgeOne Makers 全球入口与多区域 Functions，Neon PostgreSQL 保存账号、订单和 Credit 强事务账本；EdgeOne KV 仅用于非财务缓存；开发阶段使用平台地址，正式域名候选为 `knowgrove.app`，中国大陆正式节点需完成自有域名与 ICP 备案 | 仅完成选型、平台注册入口和 PRD 固化；EdgeOne 与 Neon 项目尚未完成创建，云服务尚未部署，不得描述为已上线 |
| 2026-07-30 | 后续版本（规划中） | 1.4.0 | 新增 / 商业化设计 | 增加言序云端 AI、Qwen 分层模型路由、Credit 成本定义与只追加账本、预估/预扣/结算、`¥9.9` 基础版、`¥19.9` 标准版、加量包、账号配对、托管支付页、签名 Webhook、到账恢复、退款拒付、消费预算、隐私边界和商业化验收标准 | 仅完成 PRD 设计；未修改插件行为，未开发账户、模型网关、Credit 或支付服务，不得描述为已实现或已发布 |
| 2026-07-30 | 2.5.7（开发中） | 1.3.4 | 本地交付 | 生成不包含用户设置、评论和引用数据的标准 Obsidian 安装包 `release/言序-KnowGrove-2.5.7-安装包/knowgrove-2.5.7.zip` | 生产构建通过；压缩包仅包含 `knowgrove/main.js`、`manifest.json` 和 `styles.css`，内置 Manifest 版本为 2.5.7，SHA-256 为 `74372cc1ee411dc636d2ed0cbdcb79dbd46296419f47b7451a2a7b160426a68b`；这是本地候选包，不等同于正式公开发布 |
| 2026-07-30 | 2.5.7（开发中） | 1.3.3 | 修复 / 视觉调整 | 阅读列表顶部改为自适应两列布局：未读/已读占满剩余空间，刷新按钮使用独立固定列；窄侧栏中数量换到分类名称下方，避免状态文字、篇数和刷新按钮互相覆盖 | 175 项单元测试、类型检查、生产构建通过；当前 Vault 已部署并重载 2.5.7，同一 180.5px 内容宽度下分类与刷新按钮的覆盖从约 35.6px 降为 0，保留 6px 间距；`data.json` 哈希前后一致，Obsidian 无插件或控制台错误 |
| 2026-07-30 | 2.5.6（开发中） | 1.3.2 | 新增 / 调整 | 阅读列表中每篇文章的“三点”菜单增加“AI 解析”，直接对当前轻量链接笔记或具有可恢复原文、逐字稿的笔记发起解析，不再要求先打开文件或使用全局扫描；浏览器登录态抓到的动态页面正文可以安全写回来源 URL 完全一致的指定轻量笔记，来源不一致时拒绝覆盖 | 175 项单元测试、类型检查和生产构建通过；当前 Vault 已部署并重载 2.5.6，真实阅读列表菜单确认显示“AI 解析”；指定豆包笔记已从登录态页面取得 4269 字正文，由 CodeBuddy 完成摘要、要点、Markdown 重构和原文保留，采集状态为“已完成”，文件按规则改名为 `2026-07-29-平台型APP AI营销调研.md`；Obsidian 无插件或控制台错误，19 组设置键和 4 条引用记录保留 |
| 2026-07-30 | 2.5.5（开发中） | 1.3.1 | 新增 / 调整 | 建立通用链接解析：短链跟随重定向后按最终地址、Content-Type、Content-Disposition、页面媒体元信息和 DOM 重新分类；浏览器扩展 0.3.4 读取豆包等动态 AI 对话页的已渲染内容；手机轻量链接增加页面内嵌结构化 JSON 兜底；陌生音视频平台统一字幕优先、无字幕再下载与 Whisper 转录；解析完成后沿用新内容属性流程识别领域和主题 | 173 项单元测试、类型检查、生产构建与浏览器扩展 Manifest V3 安全检查通过；当前 Vault 已部署并重载 2.5.5，真实 HTTP 短链已完成重定向识别、文章整理、属性补齐和写回，Obsidian 无插件或控制台错误；部署未复制 `data.json`，现有 19 组设置键和 4 条引用记录保留；登录受限、私有或已失效且页面不返回正文的分享链接仍需在浏览器登录态下剪藏可见内容 |
| 2026-07-29 | 2.5.4（开发中） | 1.3.0 | 品牌 / 视觉调整 | Obsidian 插件与浏览器剪藏扩展的中文品牌统一为“言序”，英文品牌统一为“KnowGrove”；浏览器扩展目录、文档、Manifest、设置与提示文案同步改名；两插件主交互色统一为言序 App 的暖朱红 `#F24B3F`；生成 Obsidian 2.5.4 与浏览器扩展 0.3.3 双端本地安装包 | 171 项测试、类型检查、生产构建、扩展 Manifest V3 与安全检查通过；Obsidian 已部署重载，部署过程未复制或覆盖 `data.json`，当前 19 组设置键和 4 条引用记录保留，插件与控制台无错误；安装包结构、Manifest 版本和 SHA-256 已校验，尚未声明正式公开发布 |
| 2026-07-29 | 2.5.3（开发中） | 1.2.9 | 本地交付 | 生成不包含用户数据的标准 Obsidian 安装包 `knowgrove-2.5.3.zip`，压缩包内为可直接放入 `.obsidian/plugins/` 的 `knowgrove` 目录 | 构建、压缩包目录、内置 Manifest 2.5.3 和 SHA-256 `9f31b3f9701370edaed1904775ba3a1f4a856f485d175061a8a7da84fd2b8f82` 已校验；这是本地候选包，不等同于正式公开发布 |
| 2026-07-29 | 2.5.3（开发中） | 1.2.8 | 新增 / 调整 | 插件桌面端首次启用后自动检查并后台部署视频解析、语音转录和 Skill Pack；已有工具与当前 Runtime 直接复用，新版自动原子升级，自动任务与设置页手动配置共用单一安装进程并同步真实进度；自动整理新文档等待启动检查完成 | 171 项测试、构建、签名 Runtime 远端校验、当前 macOS 部署重载与无重复下载验证通过；`data.json` 的用户设置、评论和引用完整保留，仅更新环境检查时间，Obsidian 无插件或控制台错误；Windows 与 macOS Intel 干净机验收待完成 |
| 2026-07-29 | 2.5.2（开发中） | 1.2.7 | 发布 / 调整 | 发布 CNB Runtime 1.0.1，并将插件默认国内清单切换到该版本；清单支持按附件 URL 映射复用早期 Release 中 SHA-256 完全一致的二进制，变化文件留在新 Release，GitHub 1.0.1 继续作为逐组件备用源 | CNB Release、签名清单和变化组件已发布；清单验签通过，3 个平台共 21 项主下载地址均完成匿名可达与文件大小检查 |
| 2026-07-29 | 2.5.2（开发中） | 1.2.6 | 修复 / 调整 | 修复跨电脑 Bilibili 剪藏因过期 `yt-dlp`、HTTP 412 和登录字幕未命中导致的处理失败：浏览器剪藏按当前页面 BV 号重新解析 CID，读取登录态官方字幕且字幕正文请求不携带凭据；轻量链接动态检查全部真实字幕语言，无字幕才下载转录；字幕和 Whisper 输出合并为自然段；Runtime 双源改为选择最高签名版本，大文件连接中断时保留已下载部分并自动断点续传；全部模型 CLI 优先读取各自模型命令、帮助声明或本机缓存，MiniMax 支持下拉模型覆盖 | 169 项测试、浏览器扩展静态检查、插件构建与本机部署通过；指定 Bilibili 视频已完成匿名“无公开字幕 → 下载音轨 → Whisper → AI 自然分段”和登录态 `ai-zh` 字幕 1,201 字符提取两条实测；本机已从远端完整下载 620,187,419 字节并启用 Runtime 1.0.1，CNB 1.0.1 待同步 |
| 2026-07-29 | 2.5.1（开发中） | 1.2.5 | 修复 / 调整 | CodeBuddy 模型下拉改为读取本机 CLI `--help` 公布的模型 ID，删除遗留的 WorkBuddy `default/sonnet/opus` 回退值，并随 CLI 更新动态刷新；未指定模型时明确标记为跟随 CodeBuddy 默认模型 | 161 项测试与构建通过；当前 macOS 已部署重载，并实测从 CodeBuddy 2.128.1 检出 11 个模型、下拉选项一致、控制台无错误 |
| 2026-07-29 | 2.5.1（开发中） | 1.2.4 | 新增 / 调整 | 自动整理组件配置增加运行包总大小、实时下载进度、检查/下载/校验/配置阶段反馈，并在完成后展示网页、视频、语音与 AI 四项能力状态 | 160 项测试与构建通过；当前 macOS 已部署重载，591 MB 总包、复用流程、四项状态、设置页视觉与控制台无错误均已实测 |
| 2026-07-29 | 2.5.1（开发中） | 1.2.3 | 删除 / 迁移 | 删除 WorkBuddy CLI 提供方和独立“测试模型”配置检查；历史 `workbuddy-cli` 选择自动迁移为 CodeBuddy CLI，保留自动状态检测与手动重新检测 | 159 项测试与构建通过；当前 macOS 已部署重载，设置页 DOM、提供方列表和控制台无错误 |
| 2026-07-29 | 2.5.1（开发中） | 1.2.2 | 修复 | 修复 Obsidian GUI 无法继承终端 PATH 时误报 Claude Code 等本地 CLI 不存在；统一检测与执行路径，并补充 macOS/Linux 登录 Shell、Claude 原生安装、Windows 原生与 npm/WinGet 常见目录 | 158 项测试、构建、当前 macOS 插件重载与本机 CLI 检测通过；目标电脑 Claude 实测待完成 |
| 2026-07-29 | 2.5.0（候选） | 1.2.1 | 发布准备 | 生成统一可分发文件夹，包含 Obsidian 插件 2.5.0、Chrome / Edge 剪藏扩展 0.3.1、安装说明和 SHA-256 校验文件；Runtime 继续由 CNB 主源自动下载 | 插件构建与浏览器扩展检查通过；分发目录结构和压缩包内容已校验 |
| 2026-07-29 | 2.5.0（候选） | 1.2.0 | 发布 / 调整 | 发布 CNB 国内 Runtime 1.0.0（17 个稳定附件），插件默认改为 CNB 主清单、GitHub 备用清单，并增加既有清单重签工具 | CNB 匿名下载、清单 SHA-256、Ed25519 签名、21 项远程组件与 Obsidian 插件内 `cnb.cool` 主源审计均已实测；完整干净机验收待完成 |
| 2026-07-29 | 2.5.0（候选） | 1.1.0 | 新增 / 发布 | 完成 Runtime 1.0.0 三平台构建、签名清单、GitHub 公共分发和 macOS Apple Silicon 插件内远端安装；CNB 国内主源等待账号实名认证 | 已部署并完成 macOS 实测 |
| 2026-07-28 | 2.4.26 | 1.0.0 | 建立基线 | 汇总 KnowGrove 当前全部产品能力、数据边界、Runtime 真实发布状态、验收标准和后续 PRD 维护规则 | 已建立文档；插件功能以当前实机版本为准 |
