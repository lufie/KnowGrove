# KnowGrove Obsidian 插件公开交付契约

> 本文件只描述公开 Obsidian 插件及其公开本机桥接契约。跨端能力、桌面 App、浏览器扩展和云服务的统一产品定义由私有平台总 PRD 维护。

## 1. 当前基线

| 项目 | 当前状态 |
| --- | --- |
| 插件版本 | 2.8.18 本地候选 |
| 当前公开版本 | 2.8.11 |
| 最低 Obsidian | 1.13.0 |
| 公开仓库 | `lufie/KnowGrove` |
| 交付契约版本 | 2.0.0 |
| 发布状态 | Draft PR #18；尚未合并或发布 2.8.18 |

状态定义：代码、构建或本机安装通过只能称“候选”；只有 GitHub Release 地址、重新下载安装和干净环境验收均存在时才能称“已发布”。

## 2. 公开与私有边界

本公开仓包含：

- Obsidian 插件的完整源码、样式、测试、构建和 Release 工作流。
- Vault 集成、阅读管理、知识工作台、评论引用、编辑增强和附件治理。
- 监听 `127.0.0.1` 的公开本机桥接协议、配对和任务状态。
- 插件侧 Runtime 检测、下载验证和兼容性契约。

本公开仓不包含浏览器扩展、独立桌面 App、云服务和私有 Runtime 实现。它们只通过版本化、可审查的协议与插件协作。历史上已经公开的浏览器/云端提交不能因当前目录删除而重新视为机密。

## 3. 插件能力

### 3.1 收集与处理

- Capture Center 支持网页、批量链接、本地音视频和录音任务。
- 网页文章保留正文、图片和来源；错误页、登录壳和空 SPA 不得伪装成功。
- 视频与音频优先使用字幕，无字幕时回退本地 Whisper；保留原语言逐字稿，分析结构跟随界面语言。
- 任务支持取消、失败重试和模型/CLI 切换接力；已安全保存的原始材料应复用，避免重复下载。
- 录音停止时先保存音频和最小笔记，再在后台完成转录与 AI 整理。

### 3.2 Read It Later

- 指定收集目录、阅读状态、最近文件、筛选和文末自动标记已读。
- 新材料只补齐缺失的核心属性，不覆盖用户已有或未知字段。
- macOS 可配置库外 Markdown 的默认打开、安全导入、导入目录和导入后源文件处理。

### 3.3 属性与知识工作台

- 文件名、阅读状态、创建日期和用户确认的分类规则可审计、预览和批量处理。
- AI 属性只能返回受约束字段和值；密钥样式字段不得进入 Prompt。
- 主题索引、研究课题、PDSA、项目/生活空间和研究创作都使用可编辑 Markdown、Wikilink、Block ID 和 Base。
- AI 只能使用已提供证据路径，不能发明来源或覆盖非托管正文。

### 3.4 评论、引用与编辑

- 选区评论和整块引用使用原生块 ID、Wikilink 和稳定标记，支持源内容变化后的保守修复。
- 类 Word 编辑增强只接管明确规则；普通段落保持 Obsidian 原生行为。
- 附件治理先预览、再确认；共享附件、Canvas/Base、排除目录和历史引用证据必须受到保护。

## 4. 本机桥接契约

- 服务只监听 `127.0.0.1:47831`，首次连接必须在 Obsidian 内确认。
- 浏览器端负责采集当前页面与用户主动授权的当前 origin 会话；插件负责权威任务、处理、Vault 写入和最终文件回读。
- Cookie、授权头和临时媒体地址仅用于当前任务，不写入 Vault、历史或持久配置。
- 任务统一表现为排队、检查、处理、写入、完成、失败或取消；“完成”必须能回读真实 TFile。
- 协议字段或状态变化必须版本化，并同步更新公开测试和私有兼容检查。

## 5. 数据与安全

- Vault 数据、本地媒体、录音、评论、引用和设置默认留在本机。
- 本地 CLI 的 Prompt 会发送给用户选择的模型提供方；插件必须在设置中说明范围。
- 不提交 API Key、Token、Cookie、签名材料、模型或私有 Runtime 二进制。
- 不绕过验证码、付费墙、DRM、平台权限或站点限制。
- 删除和迁移优先使用 Obsidian/macOS 回收站，并保留历史数据与可恢复路径。

## 6. 2.8.18 当前验收状态

已完成：

- 官方 lint、TypeScript、248 项测试、生产构建和 GitHub CI。
- 外部 Markdown 配置、真实默认打开链路和设置页视觉验收。
- 文末自动标记已读的真实 Vault 运行验收。
- 浏览器配对恢复、任务取消/模型接力、文件存在性回读及抖音弹层 URL 标准化的插件侧实现。

仍待完成：

- 公开仓 Draft PR #18 合并与公私边界最终复核。
- Chrome 实际加载扩展 0.3.13 后的授权、任务终态和 Vault 回读。
- 小红书、抖音、TikTok、西瓜视频和微信视频号的真实登录态端到端证据。
- 麦克风停止、悬浮录制和占用恢复的真实硬件验收。
- 2.8.18 GitHub Release 与从分发地址重新安装的干净环境验收。

## 7. 版本与 Release

- 插件标签必须与 `manifest.json` 完全一致，例如 `2.8.18`，不得添加产品前缀。
- Release 必须包含 `main.js`、`manifest.json` 和 `styles.css`，并通过 lint、类型检查、测试、构建、版本校验和来源证明。
- 本地 `release/` 只保留最新一个已经完整生成并验证的插件候选；新候选失败时不得先删除旧候选。
- 本地单版本规则不自动删除 Git 标签和远端正式 Release。旧版仍可能用于兼容安装、回滚和审计。

## 8. 文档维护

- 本文件只保留公开插件当前能力、边界、验收和最近重大变化，不再追加完整历史流水账。
- 详细测试步骤、截图、校验和和逐站结果放入 `docs/`；旧过程归档到私有平台历史文档。
- 插件专属变化更新本文件；影响录音、转录、任务、Markdown 输出、隐私或桥接协议的变化，还必须同步更新私有跨端总 PRD 的能力矩阵。

## 9. 变更记录

| 日期 | 契约版本 | 变更 | 状态 |
| --- | --- | --- | --- |
| 2026-08-15 | 2.0.0 | 将千行混合 PRD 收敛为公开插件交付契约；跨端能力迁入私有统一总 PRD；补充单版本本地 Release 规则 | 2.8.18 Draft 候选 |
